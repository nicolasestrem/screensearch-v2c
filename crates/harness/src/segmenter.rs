//! The pure candidate segmenter: ordered frame metadata in, session spans out. The
//! `where_was_i` context key (`03 §7b`) generalized with taxonomy tool identity and a
//! gap/close rule, per `03 §7e`. Filled in Task 7.
