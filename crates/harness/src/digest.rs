//! The human-readable per-day digest: consecutive same-`app_hint` frames collapsed into a
//! readable context-run timeline the maintainer labels from. Deliberately NOT the candidate
//! segmenter, so the labels stay anchor-free (unbiased). Filled in Task 5.
