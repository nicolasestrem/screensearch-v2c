# 07 — Known Gaps

> Where the **spec is silent** and a decision was needed, plus **manual interventions** still
> required (with owner + deadline). The agent appends here instead of guessing (`04 §5`). Empty
> until the build hits a gap.

| # | Date | Gap (spec was silent on…) | Resolution / decision | Owner | Needed by |
|---|---|---|---|---|---|
| 73 | 2026-06-30 | **Degrade-to-text retention — deferred follow-ups** (`feat/degrade-to-text-retention`). Native-WebP storage + degrade-to-text retention landed (schema v7 `image_purged`; readability + bounded image growth). Three items were scoped out **by user decision** during the brainstorm. | **Deferred, recorded:** (a) **✅ DB-side growth — resolved (current arc, `fix/degrade-to-text-db-growth`).** For a purged frame the per-word `text_spans` (which power `FrameReconstruction` — *not* search) are now **merged to per-line** (`crates/store/src/records.rs::merge_spans_to_lines` / `merge_frame_spans_to_lines`): at purge time in the retention sweep, plus a one-time watermark-gated backfill (`merge_purged_spans_once`, `maintenance.purged_spans_merged`) for the pre-existing backlog. Reclaims ~80% of span rows while the Moment view still renders a line-level layout; search is untouched (FTS reads `content_text`, the vector arm reads `embeddings`). Chosen over pruning spans outright, which would have gutted `FrameReconstruction` for exactly the purged frames it targets. **PR #67 review hardening (2026-07-01):** the retention sweep now degrades a frame **atomically** via `records.rs::degrade_frame_to_text` (merge spans→per-line **and** set `image_purged` in one transaction) instead of a separate `purge_frame_image` + non-fatal merge; a transient merge failure can no longer leave a frame flagged purged with its per-word rows stranded on a row the sweep (filtered on `image_purged = 0`) would never revisit — on error the flag stays unset and the whole frame retries next sweep (Codex P2). The reviewer's N+1 "bulk-`IN`" suggestion for the backfill was **declined** and recorded as `TODO.md` TODO-2: embedded SQLite has no network round-trip, neither path is hot, and a bulk transaction would forfeit the per-frame failure isolation that lets the backfill converge (one busy frame would roll back a whole 256-batch). **Residual:** coarser `text_filter_stats` / `backfill_filter_version` granularity for past-retention frames (accepted); the FTS/embeddings share of DB growth is kept (needed for search). (b) **Lossy WebP / AVIF** — would shrink *recent* files further but needs a new C dep (`webp`/libwebp-sys) or slow `ravif`; lossless (no new dep) chosen. `storage.jpeg_quality` is inert until a lossy codec lands. (c) **Vision / `embed_image` on degraded frames** — a purged frame has no image file, so on-demand vision tagging or a manual `embed_image` on a >retention-old frame will fail to load the image (embed_image is only auto-enqueued at capture, long before purge; on-demand vision targets recent frames — both acceptable). | agent+user | (a) ✅ current arc; lossy codec optional |
| 72 | 2026-06-29 | **llama.cpp multimodal host-memory leak (build 9842).** ~149 MB committed host RAM leaked **per vision inference** (VRAM flat; isolated 60-frame test 2026-06-29: 6.8 → 16.3 GB committed). Freed only on process exit. | **Open (upstream).** Local mitigation shipped 0.2.1: the **sidecar recycle valve** (`sidecar.recycle_enabled` default true; `sidecar.recycle_rss_mb` default 0=auto, explicit clamp 8192–131072 MiB; the 8 GiB floor clears the 8B vision warmup baseline so it can't recycle-per-request) recycles llama-server at a committed-RAM ceiling. **The real fix is upstream** / a newer bundled llama.cpp build — re-run the 60-frame test when it lands. Shipped-mitigation detail + small-RAM behaviour: `06` #15 and the `08`/CHANGELOG v0.2.x archives. | agent | open (upstream; local mitigation ✅ 0.2.1) |
| 71 | 2026-06-29 | **UIA text (#48) froze Chromium/Electron apps under interactive load** (default `capture.uia_text_enabled` on): every trigger ran an uncached **raw-view** DFS of the foreground a11y subtree, issuing thousands of synchronous cross-process COM calls the *target* app had to service, so the browser hung (no CPU spike; threads parked on COM marshalling). | **Mitigation shipped 0.2.1** (`fix/uia-chromium-hang`): don't run UIA on scroll/click, single in-flight walk + bounded channel, control-view (not raw-view) walk, `TextPattern` gated/capped, an input-activity gate (`capture.uia_suppress_during_input_ms`), and clamped Settings knobs — the hang is gone. Full detail: the `08`/CHANGELOG v0.2.x archives. **✅ Efficiency lever resolved (current arc, `fix/uia-findall-buildcache`).** Live verification shaped the design: (1) a single `FindAllBuildCache(TreeScope_Subtree)` was tried and **rejected** — one uninterruptible call, ~1.4 s on a large window, overran the hard timeout; (2) a `FindAllBuildCache(TreeScope_Children)` BFS was tried and **rejected** — a single wide-node child fetch still overran the budget on a VS Code-scale window; (3) shipped: the granular DFS keeps the old walker navigation (small, deadline-interruptible calls) but batches each node's ~5 `Current*` property reads into **one `BuildUpdatedCache`** call + cached getters (~2.5× fewer COM calls) — same boundedness as the shipped DFS, no wide-node cliff. `TextPattern` stays live/gated/capped; `Name` reads from the cache, and the field `Value` is read **live** *after* the password/offscreen guard (never prefetched for masked/hidden fields — PR #68 Codex P2). **Live-verified** on Chrome (`primary_source='uia'`, 1186–1748 chars, omnibox URL captured, no over-budget warnings) + the `#[ignore]`d test bounded on a heavy window that timed out the bulk-fetch variants. Four adversarial/external-review defects fixed en route: cache the `ValueValue` *property* — later superseded by the live-value fix below (else `CachedValue()` silently drops edit-field text); descend past a `BuildUpdatedCache` failure (else a transient timeout prunes a whole subtree); coverage parity (descend into everything, like the old DFS); and **don't prefetch field values before the privacy guard** (Codex P2 — `ValueValue`/`ValuePattern` removed from the batch, `Value` read live post-`should_emit`). | agent | ✅ current arc (efficiency lever shipped; hang mitigation ✅ 0.2.1) |
| 8 | 2026-06-21 | The vector arm post-filters `time_range` *after* the KNN (over-fetch `pool`, then filter on the join), so in-range matches that fall beyond the top-`pool` nearest vectors can be **missed** (recall under-counts on tight time windows). | **✅ Resolved (current arc, `fix/vector-arm-time-range-recall`):** pushing the filter *into* `MATCH` stays blocked (sqlite-vec 0.1.9 can't filter inside a KNN; the 0.1.10-alpha ships a broken amalgamation), so `crates/store/src/search.rs::text_knn_in_range` now **adaptively escalates `k`**. An unbounded range keeps the single `k = pool` pass; a **bounded** range re-runs the KNN with a geometrically larger `k` (×`KNN_ESCALATION_FACTOR`=8, ceiling `MAX_TIME_RANGE_KNN`=20 000) until the pool fills with in-range frames, the vector table is exhausted (KNN returned `< k` rows), or `k` hits the ceiling. Covered by `vector_arm_finds_in_range_match_buried_beyond_pool` (buries the only in-window match at rank 56 behind 55 nearer out-of-window vectors → red without the fix, green with it). **Residual:** a pathologically tight window on a very large table *past* the 20 k ceiling can still under-count (bounded, documented). Perf held — 10k-frame fixture **p95 80 ms** < 200 ms (up from ~32 ms because time-windowed queries now run a 2nd escalation pass; still well within budget). | agent | ✅ current arc |
| 26 | 2026-06-22 | **Packaging (DoD §13.9)** — `02 §5`/`03 §13.9` wanted an Inno installer + portable ZIP, but Tauri 2 ships NSIS/MSI natively, not Inno. | **✅ Resolved (current arc, `chore/cancel-inno-and-a11y-matrix`):** ships a standalone **unsigned NSIS installer** via real `tauri build` (`bundle.targets=["nsis"]`); **Inno / MSI / portable ZIP formally dropped** by user-approved full spec sweep — every live ref (`00` §A/§G, `01`, `02` P5, `03` §11 + DoD §13.9, `docs/ARCHITECTURE.md` §12, `.github/workflows/ci.yml`) rewritten to NSIS, and **DoD §13.9 re-scoped to NSIS and met**. `onnxruntime.dll` bundling is **moot** — `ort` static-links ONNX Runtime into the exe (`download-binaries`, no `load-dynamic`/DLL). **Code-signing is now the lone open packaging item** (SmartScreen warns on launch) — tracked under "Manual steps" below. | user+agent | ✅ resolved (installer shipped v0.1.0; code-signing tracked separately) |
| 42 | 2026-06-23 | **P5 keyboard/focus matrix incomplete** — live route reachability was observed, but WebView2 focus extraction stayed mostly at the document level and Timeline keyboard scrub/open did not navigate during the audit. | **✅ Mostly resolved (current arc, `chore/cancel-inno-and-a11y-matrix`).** Audit of all six surfaces (Explore agents) confirmed the baseline already meets `UI_REFERENCE.md` §7 (Timeline `role=slider` arrows/Home/End/Enter; Command Palette combobox+listbox + focus trap; Settings labelled controls). Small real fixes shipped + a **reliable focus probe** run via a live Playwright/Chromium walkthrough against the Vite dev server (a focus probe reading `document.activeElement`): **(1)** NavRail roving-tabindex (Arrow/Home/End move focus, wrap both ends) + `aria-current="page"` — **live-verified** (one tab stop, follows focus, re-seeds to the active route); **(2)** Command Palette focus restoration on close — **live-verified** (`Esc` → focus back on the ⌘K trigger); **(3)** Recall Ask focus-to-answer on stream completion and **(4)** Settings `<Panel group>` (`role="group"` + `aria-labelledby`, the ARIA equivalent of fieldset/legend) — **build + code-verified** (both need live backend data the plain-Chrome/IPC-less probe can't supply). **Residual (low-risk manual):** a populated-data keyboard pass of Timeline scrub/open, Recall results, and Moment actions in the real WebView2 app — those handlers are pre-existing + ARIA-correct + unchanged; Playwright can't attach to WebView2 and plain Chrome has no captured frames, so it stays a manual confirmation. | agent | ✅ mostly resolved (fixes shipped + Chrome-verified; data-backed live keyboard pass = manual residual) |
| 43 | 2026-06-23 | **No deterministic route loading/error-state triggers for audit verification** — live loading/error states could not be forced for every P5 route without fabricating data or changing product code. | **Resolved (current arc, `chore/archive-known-gaps`):** a **dev-only** `?__devState` URL param (`loading` / `error`) forces any P5 route into that state, applied centrally at the `queries.ts` `useQuery` seam (all 17 read hooks) and **stripped from production builds** (`import.meta.env.DEV`; verified absent from emitted `dist`). It forces query-result *flags* only — empty/partial/populated stay driven by real data, so **no mocks in the production path**. Documented in `docs/DEV_STATE_OVERRIDE.md`. | agent | ✅ current arc |
| 44 | 2026-06-23 | **VLM image-path evidence was not visible in app logs** — a Tauri log scan found no request line showing the image input path for a `vision_tag`, despite the job/`vision_analysis` row proving live tagging. | **Resolved (current arc, `chore/archive-known-gaps`):** the kernel `vision_tag_outcome` now logs `frame_id` + the relative capture path at **`info`** — the default `EnvFilter` level, so it reaches `screensearch.log`; a `debug!` line was exactly why the scan found nothing — immediately before each `vision.analyze`. Privacy-safe: never the image bytes/data-URL, OCR text, app/window title, or model reply (`init_tracing`'s no-screen-content contract). | agent | ✅ current arc |
| 46 | 2026-06-24 | **Stall-abort can't cancel hf-hub's detached chunk tasks** (PR #26, Codex P1) — the model-download stall watchdog calls `task.abort()` on the outer fetch task, but `hf-hub` 0.4.3 starts its own per-chunk `tokio::spawn`s internally; aborting the parent drops their join handles without stopping them, so a chunk task on a (briefly) revived socket could still write the `.sync.part` after the UI reports `Failed`. | **Open (bounded, deferred):** the blast radius is contained — (a) resume reads the **committed counter** in the `.sync.part` trailer as the authoritative offset, so stale writes past it are re-fetched/overwritten next attempt (committed data isn't corrupted); (b) downloads are **serialized per lane**, so a user-initiated retry never runs a second `ModelDownloader` fetch concurrently. A real fix means replacing the high-level `ApiRepo::download_with_progress` with a cancellable chunked downloader we own (or forking hf-hub) — a larger change than this PR's scope. **Update (0.2.0 PR8):** the default path is now the chunked downloader, whose chunk futures are awaited *inside* the fetch task (`buffer_unordered`, no detached `tokio::spawn`), so `task.abort()` cancels every in-flight chunk — **no orphaned writers on the chunked path**. This gap now applies only to the single-stream hf-hub **fallback** (range-less servers), where the bounded mitigations above still hold. | agent | mostly resolved (chunked path); remains for hf-hub fallback only |
| 50 | 2026-06-25 | **Scheduled / saved reports (deferred beyond PR6)** — PR6 ships on-demand Daily/Weekly/Custom reports with **no** saved-report table; recurring scheduled reports + persistence are out of 0.2.0 scope (`03 §8b`). | **Deferred** (`docs/0.2.0.md` PR6): revisit after the reports feature lands; would add a saved-report/schedule store + a scheduler. | agent | post-0.2.0 |
| 58 | 2026-06-25 | **OPEN — live multi-DPI / secondary-monitor `target_rect` acceptance check (PR3)** — gap #54 marks the `target_rect` DPI mapping *code* resolved, but the runtime behaviour can only be confirmed on real hardware: a `npm run tauri dev` session spanning **≥2 monitors at different DPI scales**, verifying the foreground window's normalized rect maps to the correct monitor band (background/system suppression fires on the right monitor, never the wrong one). CI cannot cover this. Tracked as its own row so it stays visible **after** PR #32 merges and #54 reads ✅. | **Open (manual, owner = user/maintainer):** run the multi-DPI check on a real desktop session; a wrong rect can only *under*-suppress (recoverable via `include_chrome` + preserved `raw_text`), never silently lose content, so it does not block merge — but the check must be done before relying on background/system suppression on multi-monitor setups. Update this row to ✅ once verified. | user | open (before multi-monitor reliance) |
| 69 | 2026-06-26 | **PR8 audit: stale bitmap can still outlive a pre-existing truncated `.part`.** PR8 correctly resets a stale `.parts` bitmap when `open_preallocated` creates a brand-new `.part` (covered for complete and partial manifests). The audit found a narrower external-cleanup edge case: if a cleanup tool truncates an existing `.part` without deleting it, `set_len` can re-expand the file with zeros while a header-matching stale bitmap still marks chunks done. | **Open narrow hardening gap, not a PR8 release blocker:** live dev-exe resume from a normal interruption worked, and the fresh missing-`.part` corruption fixed by PR8 is covered by tests. A future hardening pass should compare the pre-resize `.part` length or otherwise invalidate manifests when the existing partial cannot prove its done-marked ranges contain real bytes. When HF advertises `X-Linked-ETag`, sha256 still catches bad assembly; the length-only/no-hash case is the residual risk. Renumbered from duplicate #66 so #66 remains the PR3 audit fix. **Resolved (current arc, `fix/download-resume-hardening`):** `open_preallocated` now reports `unbacked` = `pre_existing_len != total` (instead of "created"), so any `.part` not already holding *exactly* `total` real bytes — externally truncated (then `set_len`-re-grown with zeros) **or** corruption-grown larger — discards its stale `.parts` bitmap and refetches every chunk. Provably no false positives on a legitimate resume: a real interrupted `.part` is always preallocated to exactly `total` (confirmed by `resume_skips_already_completed_chunks`), so only these anomalies are flagged. Covered by `truncated_part_discards_stale_partial_manifest` (red→green) and `oversized_part_discards_stale_manifest` (the `> total` case, added per a PR #62 review note). The same-length in-place-corruption case stays the residual length-only/no-hash risk (caught by sha256 when `X-Linked-ETag` is advertised). | agent | ✅ current arc |

> Resolved v0.1.0-era rows (2026-06-21..2026-06-24) → `specs/archive/07_KNOWN_GAPS.v0.1.0.md` (ids preserved).
> Resolved + retired 0.2.x rows, and the resolved-engineering-decisions list → `specs/archive/07_KNOWN_GAPS.v0.2.x.md` (original `#N` ids preserved). The live table holds only still-open + current-arc rows.

Manual steps still required (e.g. signing certs, first-run model download, CI secrets):
- **First-run model download** — embedding models (P3) auto-download via fastembed into
  `<app-data>/models/fastembed`. **P4:** the `llama-server` Vulkan binary auto-downloads into
  `<app-data>/sidecar/llama` at launch (off-thread; `sidecar` readiness Initializing→Ready/
  Unavailable), and the vision/answer GGUF (+ mmproj) auto-download via `hf-hub` into
  `<app-data>/models/<lane>/<tier>` **lazily on first request** (gaps #14/#15). Nothing is bundled.
  No download-progress %% yet (readiness is coarse Initializing→Ready); a percentage is a P5 nicety.
- **Code-signing certificate** for the installer — P5 packaging. Recommended path for this MIT OSS
  project (cheapest → most turnkey): **SignPath Foundation** (free Authenticode signing for
  qualifying OSS) → **Azure Trusted Signing** (~US$10/mo, Microsoft-run, builds SmartScreen
  reputation) → **Certum Open Source Code Signing** (cheap annual cloud cert for OSS devs). Note:
  since 2023 all OV certs require a hardware token or cloud HSM, so a plain file-based cert is no
  longer available; budget for that. Decide owner before P5.
- **TODO (0.1.1) — PR #27 review follow-ups (deferred from v0.1.0; user chose to ship as-is):**
  - ✅ **Done (current arc, `fix/download-resume-hardening`) — Codex P2: re-check cache before retrying
    a locked download.** The single-stream hf-hub fallback's lock-retry was folded into `fetch_one`,
    and the clean-layout + HF-cache fast paths were extracted into a `place_if_cached` helper that is
    re-checked **after each `LockAcquisition` backoff**: if the lock holder finishes during the sleep,
    the loser copies the now-present finalized blob into the clean layout and short-circuits instead of
    re-downloading or colliding on publish. The bounded backoff was also extended (per-attempt cap +
    more attempts → ~5 min total patience, vs the old ~20 s that could abandon a real multi-GB
    download), while the cache re-check exits the instant the holder is done. (`place_if_cached` unit
    tests cover the dest-present short-circuit and the miss path.) *Note:* this closes the PR #27
    Codex-P2 follow-up only; the separate `#46` row — orphaned **detached** chunk writers in the same
    single-stream fallback — stays open (it needs replacing hf-hub's high-level downloader).
  - ✅ **Done (current arc, `chore/cancel-inno-and-a11y-matrix`) — Gemini: single-instance focus.**
    The `tauri-plugin-single-instance` callback (`src-tauri/src/lib.rs`) now calls `window.show()`
    before `unminimize()`/`set_focus()`, so a hidden / tray-minimized window is restored (not just
    unminimized) on a second launch.
