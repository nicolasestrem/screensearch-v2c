# 07 — Known Gaps

> Where the **spec is silent** and a decision was needed, plus **manual interventions** still
> required (with owner + deadline). The agent appends here instead of guessing (`04 §5`). Empty
> until the build hits a gap.

| # | Date | Gap (spec was silent on…) | Resolution / decision | Owner | Needed by |
|---|---|---|---|---|---|
| 8 | 2026-06-21 | The vector arm post-filters `time_range` *after* the KNN (over-fetch `pool`, then filter on the join), so in-range matches that fall beyond the top-`pool` nearest vectors can be **missed** (recall under-counts on tight time windows). | **Deferred (tracked):** acceptable for P1 (FTS arm is unaffected and carries the live path; vec arm is dark until P3). Revisit in P3 with real embeddings — e.g. push the time filter into the KNN via a `vec0` partition/metadata column, or widen the pool adaptively. **P3 update:** left open — unobserved to cause a problem at the perf-fixture scale (32.6 ms p95 with `pool = max(limit·5, 50)`); revisit only if recall on tight time windows proves insufficient with real data. | agent | open (P3→P5) |
| 26 | 2026-06-22 | **Packaging (DoD §13.9)** — `02 §5`/`03 §13.9` want an Inno installer + portable ZIP, but Tauri 2 ships NSIS/MSI natively, not Inno. | **Partially resolved (user-approved, v0.1.0, 2026-06-24):** ships a standalone **unsigned NSIS installer** via real `tauri build` (`bundle.targets=["nsis"]`); MSI / Inno / portable ZIP not pursued. `onnxruntime.dll` bundling is **moot** — `ort` static-links ONNX Runtime into the exe (`download-binaries`, no `load-dynamic`/DLL). **Code signing remains OPEN** (SmartScreen warns on launch). DoD §13.9 partially met. | user | partial — installer shipped (v0.1.0); signing open |
| 40 | 2026-06-23 | **Live OCR did not preserve underscore-delimited audit token exactly** — the live Notepad capture extracted normalized token text and `LOCAL SYNTHETIC EVIDENCE ONLY`, but not the full `SSV2C_AUDIT_20260623_LOCAL_EVIDENCE_0SPP1C` string with underscores and random suffix. | **Open:** acceptable for the audit outcome because visible synthetic evidence, search, Moment, and Ask were proven with normalized text. Follow-up options: adjust audit-token format to OCR-friendly separators, improve OCR post-processing for identifiers, or define the acceptance bar around normalized OCR text. | agent | open (post-audit OCR follow-up) |
| 42 | 2026-06-23 | **P5 keyboard/focus matrix incomplete** — live route reachability was observed, but WebView2 focus extraction stayed mostly at the document level and Timeline keyboard scrub/open did not navigate during the audit. | **Open:** complete manual or automated keyboard verification for NavRail, Command Palette, Timeline scrub/open, Recall search/ask, Moment actions, and Settings controls with a reliable focus probe. | agent | open (post-audit a11y follow-up) |
| 43 | 2026-06-23 | **No deterministic route loading/error-state triggers for audit verification** — populated/empty/partial states were observed where feasible, but live loading/error states could not be forced for every P5 route without fabricating data or changing product code. | **Open:** add supported test hooks, fixtures, or documented manual procedures for route state verification that do not rely on mocks in production paths. | agent | open (post-audit UI QA follow-up) |
| 44 | 2026-06-23 | **VLM image-path evidence was not visible in app logs** — the persisted frame path, completed `vision_tag` job, `vision_analysis` row, and sidecar `--mmproj` command line proved live VLM tagging, but the Tauri log scan found no request log line explicitly showing the image input path. | **Open:** add privacy-safe VLM request logging that records the frame id and relative image path or image payload source without exposing unrelated screen contents. | agent | open (post-audit observability follow-up) |
| 45 | 2026-06-23 | **No supported isolated app-data override for destructive-free live audits** — the audit prompt forbade deleting or resetting `%APPDATA%\app.screensearchv2c.desktop`, so verification ran against the real app-data directory with synthetic visible content only. | **Open:** consider a documented, supported app-data override for future audits so live verification can run in an isolated profile without touching the user's real ScreenSearch data. | user+agent | open (future audit ergonomics) |
| 46 | 2026-06-24 | **Stall-abort can't cancel hf-hub's detached chunk tasks** (PR #26, Codex P1) — the model-download stall watchdog calls `task.abort()` on the outer fetch task, but `hf-hub` 0.4.3 starts its own per-chunk `tokio::spawn`s internally; aborting the parent drops their join handles without stopping them, so a chunk task on a (briefly) revived socket could still write the `.sync.part` after the UI reports `Failed`. | **Open (bounded, deferred):** the blast radius is contained — (a) resume reads the **committed counter** in the `.sync.part` trailer as the authoritative offset, so stale writes past it are re-fetched/overwritten next attempt (committed data isn't corrupted); (b) downloads are **serialized per lane**, so a user-initiated retry never runs a second `ModelDownloader` fetch concurrently. A real fix means replacing the high-level `ApiRepo::download_with_progress` with a cancellable chunked downloader we own (or forking hf-hub) — a larger change than this PR's scope. **Update (0.2.0 PR8):** the default path is now the chunked downloader, whose chunk futures are awaited *inside* the fetch task (`buffer_unordered`, no detached `tokio::spawn`), so `task.abort()` cancels every in-flight chunk — **no orphaned writers on the chunked path**. This gap now applies only to the single-stream hf-hub **fallback** (range-less servers), where the bounded mitigations above still hold. | agent | mostly resolved (chunked path); remains for hf-hub fallback only |
| 47 | 2026-06-25 | **Event-driven capture (0.2.0 deferral → 0.2.1)** — 0.2.0 keeps timer/idle capture; app-switch / focus / click / typing-pause / scroll-stop / clipboard / idle triggers need global input + clipboard hooks that don't exist today. | **Deferred to 0.2.1** (`docs/0.2.0.md`): own gated, opt-in PR; prefer `SetWinEventHook` (focus/foreground) + `AddClipboardFormatListener` over low-level keyboard hooks; document the privacy posture. Not a "real-time vision" violation (capture ≠ vision). **No raw keystrokes or clipboard contents are ever stored.** | agent | 0.2.1 |
| 48 | 2026-06-25 | **UIA text (0.2.0 deferral → 0.2.1)** — UI Automation text from the target window (with OCR fallback) would yield more structured, higher-fidelity `content_text` than OCR alone, but is greenfield, stateful, and thread-affinity-sensitive. | **Deferred to 0.2.1** (`docs/0.2.0.md`): own PR; needs a capability probe + a measured latency budget before wiring into capture. `primary_source`/`TextSource` already model `uia` in `03 §3b`. | agent | 0.2.1 |
| 49 | 2026-06-25 | **Smart enrichment throttle (0.2.0 deferral → 0.2.1)** — pausing `vision_tag`/`embed_image` and reducing `embed_text` concurrency under sustained CPU/GPU pressure (capture/OCR/storage never pause) needs a pressure probe that doesn't exist (only a flag-capability probe does, `crates/inference/src/flags.rs`). | **Deferred to 0.2.1** (`docs/0.2.0.md`): own PR; GPU utilization is NVIDIA/NVML-only, so a weak iGPU laptop is CPU-pressure-only — implement with truthful status. | agent | 0.2.1 |
| 50 | 2026-06-25 | **Scheduled / saved reports (deferred beyond PR6)** — PR6 ships on-demand Daily/Weekly/Custom reports with **no** saved-report table; recurring scheduled reports + persistence are out of 0.2.0 scope (`03 §8b`). | **Deferred** (`docs/0.2.0.md` PR6): revisit after the reports feature lands; would add a saved-report/schedule store + a scheduler. | agent | post-0.2.0 |
| 51 | 2026-06-25 | **PR2→PR3 interim: `content_text` is a raw passthrough, no backfill** — PR2 adds the `frame_text` schema before PR3's classifier, so PR2 fills `content_text` as a copy of `raw_text` (the column is `NOT NULL`). PR3's filter applies to captures from its deploy onward. | **By design (clean-DB assumption)** (`docs/0.2.0.md` PR2/PR3; `03 §3b`/`§4`): frames captured in the PR2→PR3 window keep unfiltered `content_text` and are **not** backfilled; `schema_version` still bumps 2→3. Acceptable because 0.2.0 is single-user with no old data to migrate. **Landed (PR2, 2026-06-25, `feat/0.2.0-pr2-text-signal`):** migration v3 created `frame_text`/`text_spans`/`chrome_text_catalog` and dropped legacy `ocr_text`; `insert_ocr` writes `content_text = raw_text` with `filter_version = UNFILTERED_FILTER_VERSION (0)` and `suppressed_count = 0`. PR3 wires the classifier and bumps `filter_version`. | agent | 0.2.0 PR3 |
| 52 | 2026-06-25 | **`region_bucket` geometry undefined** — `03 §3b` defines the chrome signature as `app_hint ⏐ normalized_text ⏐ region_bucket` but never the bucket's geometry. | **Resolved (agent, PR3):** the bucket is the **line centroid** placed in an N×N grid, `N = text.chrome_region_buckets` (default 8), formatted `"{floor(cy·N)},{floor(cx·N)}"`. Line-centroid (not per-word) so a whole toolbar line collapses to one cell. The signature joins fields with the non-printable separator `U+001F` (collision-safe PK); a null `app_hint` uses the empty-string sentinel. Pure, unit-tested in `crates/textfilter`. | agent | ✅ 0.2.0 PR3 |
| 53 | 2026-06-25 | **System-band threshold + multi-monitor/shell rule** — `03 §3b` says "bottom taskbar band" without numbers, and is silent on which monitor's shell counts. | **Resolved (agent, PR3):** a line is `system` only when its top-of-line `y > 0.95` (the `SYSTEM_BAND_TOP` constant), it is **short** (< `chrome_protect_min_chars`), and `target_rect` is `Some` **and** the line is **outside** it — so only the foreground monitor's shell band suppresses. These band constants are documented engineering constants (not user settings); the protect-length and seen-count are the user-tunable thresholds. Rect `None` ⇒ never `system`. | agent | ✅ 0.2.0 PR3 |
| 54 | 2026-06-25 | **`target_rect` DPI / coordinate assumption** — mapping `GetForegroundWindow` bounds into the captured monitor needs every coordinate in the same (physical-pixel) space; the spec is silent on the DPI contract. | **Resolved (agent, PR3) — highest technical risk:** capture maps the foreground window's **visual** bounds (`DwmGetWindowAttribute(DWMWA_EXTENDED_FRAME_BOUNDS)`, with an `IsIconic` guard) against the captured monitor's `rcMonitor`, normalized + clamped `[0,1]`. It assumes the process is **per-monitor-v2 DPI-aware** so `GetWindowRect`, `rcMonitor`, and the WGC texture all agree in physical pixels. `None` is the **safe fallback** (window on another monitor / minimized / unresolved) → the classifier never emits `background`/`system` without a confident rect, so a wrong rect can only *under*-suppress (recoverable), never silently lose content. **Manual multi-DPI / secondary-monitor check is the one acceptance item CI cannot cover.** | agent | ✅ 0.2.0 PR3 (manual multi-DPI check) |
| 55 | 2026-06-25 | **`filter_version` bump semantics** — `03 §3b`/`§4` define a `filter_version` watermark but not what a bump does to the existing catalog or old frames. | **Resolved (agent, PR3):** on store open, `reconcile_filter_version(FILTER_VERSION)` compares the internal `text.catalog_filter_version` KV watermark; on change it **wipes `chrome_text_catalog`** (suppression re-learns from scratch under the new logic) and rewrites the watermark, in one transaction. Old frames are **not** backfilled (extends #51, clean-DB assumption) — they keep their stored `content_text`/`filter_version`; `text_filter_stats` filters on the current `FILTER_VERSION` so stale frames don't dilute the metric. | agent | ✅ 0.2.0 PR3 |
| 56 | 2026-06-25 | **`suppressed_count` grain** — `03 §3b` stores a per-frame `suppressed_count` without defining its unit. | **Resolved (agent, PR3):** it counts suppressed **word spans** (role ∈ {system, background, chrome}), matching the `text_spans` row grain and the `text_filter_stats` denominator (per-app `suppressed_spans / total_spans`). The per-app suppression **rate** is the silent-data-loss alarm surfaced in Settings. | agent | ✅ 0.2.0 PR3 |
| 57 | 2026-06-25 | **`CaptureTrigger` not threaded into the filter** — the kickoff listed capture trigger as a classifier input, but 0.2.0 capture is timer/idle-only. | **Intentionally omitted (agent, PR3):** with only timer/idle capture there is a single trigger value, so plumbing a `CaptureTrigger` field would be dead single-variant code. The filter is trigger-agnostic; the field arrives with event-driven capture (gap #47, 0.2.1) if a trigger ever conditions classification. | agent | 0.2.1 (with #47) |
| 58 | 2026-06-25 | **OPEN — live multi-DPI / secondary-monitor `target_rect` acceptance check (PR3)** — gap #54 marks the `target_rect` DPI mapping *code* resolved, but the runtime behaviour can only be confirmed on real hardware: a `npm run tauri dev` session spanning **≥2 monitors at different DPI scales**, verifying the foreground window's normalized rect maps to the correct monitor band (background/system suppression fires on the right monitor, never the wrong one). CI cannot cover this. Tracked as its own row so it stays visible **after** PR #32 merges and #54 reads ✅. | **Open (manual, owner = user/maintainer):** run the multi-DPI check on a real desktop session; a wrong rect can only *under*-suppress (recoverable via `include_chrome` + preserved `raw_text`), never silently lose content, so it does not block merge — but the check must be done before relying on background/system suppression on multi-monitor setups. Update this row to ✅ once verified. | user | open (before multi-monitor reliance) |
| 59 | 2026-06-25 | **DST period skew in the report calendar grid (PR6)** — the report orchestrator slices a range into `DAY_MS` (86 400 000 ms) fixed-width periods from a local-midnight start, but the two DST transition days each year are 23 h / 25 h long, so a fixed-width grid drifts ±1 h relative to wall-clock midnight across a span that straddles a transition. `03 §8b` is silent on calendar arithmetic. | **Narrowed (agent, PR #33 review — Codex P2).** The UI now builds **all** range endpoints from local calendar components (`localMidnight(now, ±n)` / `localDateMidnight(value, +1)` in `ReportBuilder.tsx`) instead of adding fixed `DAY_MS`, so Daily/Weekly/Custom bounds are wall-clock-correct even on a transition day (previously `start + DAY_MS` could include/drop an hour). The only residual is the orchestrator's *internal* fixed-width per-period split: at most one period boundary lands ±1 h off true midnight on the ≤2 transition days/yr, shifting a handful of frames between two adjacent day-summaries — cosmetic; it never drops frames or skews coverage (every active period still gets its floor). True civil-calendar slicing (a `chrono-tz` day-walker) in the kernel is the fix if it ever matters. | agent | open (cosmetic) |
| 60 | 2026-06-25 | **Report structural safety bounds are named constants, not settings (PR6)** — `MIN_FRAMES_PER_PERIOD` (3), `REDUCE_FANOUT` (6), `MAX_PERIODS` (31), `MAX_REPORT_PASSES` (64), `MAX_REPORT_CITATIONS` (200) shape coverage/cost but are not user-tunable; `03 §8`/`§8b` names only the four `retrieval.*`/`reports.*` keys. | **By design (agent, PR6):** the four spec keys (`retrieval.default_top_k`, `reports.daily_top_k` = per-active-period budget, `reports.weekly_top_k` = global cap, `reports.map_reduce_min_frames` = single-pass threshold) are the user surface; the structural bounds are documented engineering constants in `crates/kernel/src/reports.rs` (preventing runaway passes/citations on a pathological range). Promotable to settings later if a real need appears; recorded here so they aren't "magic numbers". | agent | open (promotable) |
| 61 | 2026-06-25 | **Report token footer is an estimate, not the sidecar's real token count (PR6)** — the reduce single-pass gate and the UI footer use `est_tokens` (a `chars/4` heuristic), because the llama.cpp sidecar's streaming `answer()` path does not surface a per-request prompt/eval token count back to the kernel. `03 §8b` asks the report to report its model + token usage. | **Accepted (agent, PR6):** the footer shows model + **estimated** tokens (clearly an estimate) plus the *exact* structural counts that matter for trust — passes, covered/total periods, summarized/sampled frames, and a `truncated` notice. The heuristic only needs to be conservative enough to keep each pass under the flat 8192 `n_ctx` (`build_messages` is the hard per-pass safety net that truncates an oversized chunk regardless). Surfacing the sidecar's real `tokens_predicted`/`tokens_evaluated` would need a usage channel through the `AnswerProvider` streaming API — deferred. | agent | open (estimate acceptable) |
| 62 | 2026-06-25 | **PR7 audit: strict static-chrome default-search acceptance not fully closed.** Live audit on the populated DB showed default content search still returning static/app-chrome-heavy rows for `Firefox`, `Steam`, and especially `Deck`. A fresh ScreenSearch capture also kept app UI labels such as `COMMAND DECK`, `Timeline`, `Insights`, and `Settings` in `content_text` while suppressing many background spans. | **Resolved by the later PR3 audit fix (#66):** the dominant live failure was ScreenSearch indexing its own UI plus a cold-start/no-backfill window. The applied fix self-excludes own-window captures, purges pre-existing own-window frames, backfills `FILTER_VERSION` 1→2 against the warm catalog, lowers the new-install chrome threshold to 4, and hot-applies excluded-app capture changes. Evidence is summarized in `docs/0.2.0.md`, `06` #10, and #66. **Residual:** rect-None / secondary-monitor desktop chrome from other apps remains tracked by #58. | agent | ✅ resolved by #66 (residual → #58) |
| 63 | 2026-06-25 | **PR7 audit: no-evidence Ask refusals still render cited frame tiles.** A unique-token question (`SSV2C_PR7_NEVER_CAPTURED_20260625_QZX`) refused honestly, but the UI still displayed unrelated retrieved context under `CITED FRAMES`. | **Resolved (agent, PR7 follow-up):** the user chose the low-risk relabel approach. The UI now labels those tiles `Frames checked`, making them provenance for the context inspected by the model rather than evidence for a positive claim. No schema, migration, typed IPC, or model-output heuristic was added. | agent | ✅ applied (PR7 follow-up) |
| 64 | 2026-06-26 | **PR3 audit: default content search still includes static/app chrome.** The PR3-specific audit used the real dev exe (`npm run tauri dev`) and the existing DB. Baseline SQL showed content/default hits for `Firefox` 24, `Steam` 24, `Deck` 68, `Recall` 42, and `GPU Memory` 15 despite raw/content divergence on 303/313 frames. A fresh Notepad capture preserved the deliberate foreground text but also indexed `Firefox`, `Deck`, `Recall`, and `COMMAND` in default `content_text`. | **Open release blocker:** PR3's storage/retrieval plumbing passes, but strict acceptance ("default content search no longer ranks frames because of desktop icons or toolbars") does not. Fix classifier/query-time behavior or explicitly revise the 0.2.0 contract before tag. Evidence: `docs/AUDIT_0.2.0_PR3_2026-06-26.md` and local screenshots/SQL under `.playwright-mcp/pr3-2026-06-26/`. | agent | **resolved by #66** (2026-06-27) |
| 65 | 2026-06-26 | **PR6 audit: documentation drift around report/search wording and local audit artifacts.** The scoped PR6 audit found stale wording, not a PR6 runtime defect: `docs/ARCHITECTURE.md` still described a `1..=100` backend search clamp even though PR6 report/custom retrieval can use larger bounded retrieval settings; `ReportOutput.frames_summarized` had a stale internal comment tying it to cited frames; and `docs/AUDIT*.md` is ignored after the local-artifact policy, while tracked docs still referenced those audit files by path. | **Resolved / narrowed (agent, PR7 follow-up):** `ReportOutput.frames_summarized` had already been corrected by the PR6 hardening pass; this follow-up updates Architecture's search-limit wording and Testing's local-only audit artifact policy. Ignored audit markdown under `docs/AUDIT*.md` remains local evidence referenced only as a local artifact, not tracked release documentation. | agent | ✅ applied (docs cleanup) |
| 66 | 2026-06-27 | **PR3 audit fix — self-capture, backfill, cold-start, excluded-apps (resolves #64).** Live corpus replay showed the dominant default-search chrome came from ScreenSearch indexing **its own window** (`Deck`/`Recall`/`Command Deck` nav), plus a cold-start window the no-backfill design froze, plus the user-reported "Excluded Apps never applies". The catalog/backfill cannot place rect-None desktop chrome because of the classifier's deliberate "rect unknown → suppress nothing" safety invariant. | **Resolved (user-chosen "self-exclude own window"):** (a) capture skips own-process foreground frames (`is_own_foreground_window`, PID-based) + a gated one-time purge of pre-existing own-window frames; (b) `FILTER_VERSION` 1→2 backfill re-cleans sub-version frames against the warm catalog (pure monotonic `textfilter::reconcile`, re-embeds changed frames) — **reverses the no-backfill stance of #51/#52**; (c) `chrome_suppress_min_seen` default 12→4; (d) `set_settings` reloads a running capture loop when `CaptureConfig` changes. Evidence (shipped code over the audit corpus copy): default content FTS `Deck` 68→26, `Recall` 42→15, `Firefox`/`Steam` 24→15, `GPU Memory` 19→15; raw recovers all; real work terms intact. **Residual:** rect-None / multi-monitor secondary captures of *other* apps' desktop chrome still leak (overlaps #58 — needs `target_rect` persistence for a future *full* positional re-classification); some remaining hits are legitimate content that should stay searchable. | agent+user | ✅ resolved (residual → #58) |
| 67 | 2026-06-27 | **PR #41 review hardening (Codex + Gemini on the #66 fix).** Five findings on the PR3 fix branch. Three were real and fixed; two are accepted-as-is judgment calls (user-chosen) and documented here so they aren't re-flagged. | **Fixed:** (a) *Codex P1* — the backfill now deletes a frame's stale text embedding (`source='ocr'`) in the same transaction when it rewrites `content_text`, so dropped chrome can't keep surfacing the frame through `hybrid_search`'s vector arm (which fuses even with `include_chrome=false`) before the async re-embed runs — or indefinitely if text embedding is off; the `embeddings_ad` trigger cascades to the vec0 shadow. (b) *Gemini* — the read-only chrome catalog is cached by `app_hint` across the backfill instead of one `chrome_text_catalog` query per frame (N+1). (c) *Gemini* — `purge_self_captures` now `continue`s past a transient JPEG-delete failure instead of deleting the row and orphaning the file (matches the retention sweeper). **Accepted as-is (user decision):** (d) *Codex P2* — the `chrome_suppress_min_seen` 12→4 default applies to **new installs only**; existing users keep their persisted value (no settings migration, to avoid overriding a deliberate choice; the backfill + threshold still help them, and they can re-set it). (e) *Gemini* — `reload_capture`'s stop→start releases the capture lock between halves; left non-atomic because the only caller is `set_settings` and a single user can't interleave a settings save + a Stop click in the sub-ms window through the serialized Tauri path (documented in the method; refactoring the core capture lifecycle for an unreachable race was judged higher-risk). | agent+user | ✅ applied (3 fixed, 2 accepted) |
| 68 | 2026-06-27 | **PR #41 second review round (Codex + claude[bot] on commit `f8f3d83`).** Three findings on the #67 fixes; the 12→4 upgrade-path concern was re-raised with a new clamp variant. | **Fixed:** (a) *Codex P2* — `backfill_filter_version` no longer runs the whole loop under one `with_conn` (which holds `SqliteStore`'s single `Arc<Mutex<Connection>>` for the entire pass, blocking search/settings/capture inserts); it snapshots the catalog once, then processes each batch in its own `with_conn`, releasing the connection between batches. (b) *Codex P2* — `purge_self_captures` records the `maintenance.self_capture_purged` watermark **only on a full drain**, so a transient delete failure that trips the no-progress break (more likely now that #67's orphan fix leaves locked rows behind) retries next launch instead of permanently skipping. (c) *claude[bot]* — corrected the `reload_capture` doc comment: Tauri 2 async commands are **not** serialized, so the stop→start race is real (the prior "unreachable / serialized command path" rationale was wrong); the risk is still accepted (sub-ms window, no UI affordance to fire both paths at once) but now described truthfully. **Re-affirmed accepted (user):** the 12→4 default reaches new installs only — the **backfill-clamp** alternative (`min(stored,4)` for the one-time re-clean, leaving the live setting untouched) was presented and also declined; upgraders' history re-cleans at their stored threshold. | agent+user | ✅ applied (2 fixed, 1 doc-fix, 1 re-affirmed) |
| 69 | 2026-06-26 | **PR8 audit: stale bitmap can still outlive a pre-existing truncated `.part`.** PR8 correctly resets a stale `.parts` bitmap when `open_preallocated` creates a brand-new `.part` (covered for complete and partial manifests). The audit found a narrower external-cleanup edge case: if a cleanup tool truncates an existing `.part` without deleting it, `set_len` can re-expand the file with zeros while a header-matching stale bitmap still marks chunks done. | **Open narrow hardening gap, not a PR8 release blocker:** live dev-exe resume from a normal interruption worked, and the fresh missing-`.part` corruption fixed by PR8 is covered by tests. A future hardening pass should compare the pre-resize `.part` length or otherwise invalidate manifests when the existing partial cannot prove its done-marked ranges contain real bytes. When HF advertises `X-Linked-ETag`, sha256 still catches bad assembly; the length-only/no-hash case is the residual risk. Renumbered from duplicate #66 so #66 remains the PR3 audit fix. | agent | open (post-0.2.0 hardening) |

> Resolved v0.1.0-era rows (2026-06-21..2026-06-24) were moved to `specs/archive/07_KNOWN_GAPS.v0.1.0.md`; their original `#N` ids are preserved there for cross-references in `05`/`06`/`08`.

Resolved engineering decisions (spec silent on *how*, recorded for traceability):
- **ts-rs 64-bit ints → TS `number`** via per-field `#[ts(type = "number")]` (Tauri JSON wire);
  `TS_RS_LARGE_INT` env is ignored by ts-rs 10.1's macro. Guarded by `no_bigint_in_ipc_types`.
- **ts-rs `export_to`** anchors at the source-file dir here → `../../../ui/src/bindings/`.
- **CSP** set to `null` in `tauri.conf.json` for P0 dev convenience — **harden in P5**.
- ~~**§9 file logging** deferred to P1~~ — **done in P1**: console + daily-rotating file sink under
  `<app-data>/logs/` (`tracing-appender`), wired once the app-data dir is resolved at launch.
- **Store concurrency (P1):** a single `rusqlite::Connection` behind a `Mutex`, every query run via
  `spawn_blocking`. SQLite is single-writer, so this is correct and simple, and `:memory:` persists
  for the store's lifetime (clean tests). Trade-off: no reader/writer concurrency — revisit a read
  pool only if search latency demands it.
- **RRF (P1/P3 hardening, revised for PR6 reports):** `k = 60` (de-facto standard); backend search
  normalizes `SearchQuery.limit` to `1..=2,000`, then uses per-arm candidate pool
  `max(limit·5, 50)` capped at 2,000; `time_range` filters both arms (vec arm over-fetches the pool
  then post-filters on the join). The Recall UI's interactive search page remains smaller.
- **Crates (P1):** `rusqlite` 0.40 `bundled` (FTS5 is in the amalgamation — no `fts5` feature in
  0.40); `sqlite-vec` pinned to **0.1.9** (the 0.1.10-alpha line ships a broken amalgamation —
  references a missing `sqlite-vec-diskann.c`); `blake3` for `embeddings.content_hash`.
- **Schema additions (P1, non-breaking):** `UNIQUE(frame_id, chunk_index)` on `embeddings` and
  `UNIQUE(frame_id)` on `image_embeddings` to support upsert; `AFTER DELETE` triggers on the
  embedding tables purge the `vec0` shadows (FK cascade + `recursive_triggers=ON`, proven by test).
- **`JobState::Failed` reserved (P1):** the store drives pending/running/done/dead; the worker
  decides retry-vs-dead so no intermediate `failed` is needed (kept in the enum/DDL per `03 §4`).
- **Inherent store reads beyond `03 §3` (P1):** `get_frame` (backs the P5 `get_frame` command) and
  `delete_frame` (retention-purge primitive) — real APIs, not test-only, that also make the write
  paths observable.
- **`frames.activity_type` (P1, post-review):** `03 §4` documents it "filled by vision", so
  `insert_vision` now writes it (denormalized copy of `vision_analysis.activity_type`) in the same
  transaction — lets the timeline filter by activity without a join. (PR #4 review.)
- **Job no-op guard (P1, post-review):** `complete_job`/`fail_job` error on a zero-row update
  (unknown/stale id) rather than silently succeeding — upholds `03 §5`'s "never silently dropped".
- **Diff gate (P2):** `capture.diff_threshold` is a normalized `[0,1]` mean-absolute **luma**
  difference over a `32×32` nearest-neighbour grid (`diff.rs`); the first frame per monitor and any
  resolution change always pass. `content_hash` = `blake3` of the raw RGBA bytes.
- **Frame JPEG layout (P2):** `frames/day-<n>/<captured_at>-<monitor>.jpg` where
  `n = captured_at / 86_400_000` — per-day sharding **without a calendar dependency** (chose this
  over `YYYY/MM/DD` to avoid adding `chrono`/`time` in P2); stored as a path relative to app-data.
- **`CapturedFrame` refinement (P2):** added `app_hint` / `window_title: Option<String>` (a
  spec-permitted shape refinement) so the capturer carries the foreground context it already reads
  for the privacy gate, without the kernel depending on the capture impl (`03 §2`).
- **windows-rs (P2):** `windows = "0.62"`, features per crate under `cfg(windows)`. WinRT async
  uses `IAsyncOperation::join()` (windows-future 0.3 renamed the old `.get()`); SoftwareBitmap from
  bytes needs the `Storage_Streams` feature; `BOOL`/`MONITORINFOF_PRIMARY` moved namespaces.
- **COM apartments (P2):** OCR runs on a dedicated **STA** thread (`COINIT_APARTMENTTHREADED`);
  capture runs on a dedicated **MTA** thread (`COINIT_MULTITHREADED`) with a free-threaded WGC frame
  pool. No `.await` ever runs inside COM code (bridged via channels + `spawn_blocking`).
- **Lock detection (P2):** `privacy.pause_on_lock` uses an `OpenInputDesktop` heuristic — a
  non-elevated process can't open the input (secure) desktop while locked, so a failed open ⇒
  "locked" (`privacy.rs`). Simpler/robust enough vs. a `WTS` session-notification window.
- **WGC sessions (P2):** per-monitor sessions stay running and are **sampled** each
  `capture.interval_ms` (drain-to-latest + diff gate), rather than start/stop per tick. Simple and
  correct; a single-shot-per-tick optimization to cut idle GPU work can come later.
- **OCR fallback (P2):** if the WinRT engine can't be created (no language pack), the composition
  root substitutes a defensive `UnavailableOcr` and marks OCR unavailable in the kernel; the app still
  runs, but `capture_control(Start)` fails with `capture = Unavailable` before WGC opens, so no empty
  OCR rows are written and the problem is surfaced — never a silent half-working state.
- **fastembed model variant (P3):** confirmed `fastembed` 5.17.2 exposes
  `EmbeddingModel::EmbeddingGemma300MQ` (768-dim, quantized — the `MODEL_REGISTRY §3` name is exact)
  and `ImageEmbeddingModel::NomicEmbedVisionV15`; both verified by the real-model `#[ignore]` test.
  Built clean on rust-version 1.82 — no MSRV bump for `ort` 2.0.0-rc.12.
- **Query/document prompt asymmetry (P3):** EmbeddingGemma is prompt-instructed (`query:` vs
  document prefixes improve retrieval), but the `EmbeddingProvider` trait has only `embed_texts`,
  used symmetrically for both indexing and the query. Kept symmetric for P3 (simplest correct, no
  trait change); a `query_embed` path is a later retrieval-quality refinement if needed.
- **Embedder injection (P3):** `SqliteStore.embedder` is `Arc<RwLock<Option<Arc<dyn
  EmbeddingProvider>>>>` with a runtime `set_embedder`, so the composition root attaches the model
  *after* the off-thread load without rebuilding the store (no new dep; the search hot path clones
  the `Arc` out before the `.await`). `Store::set_embedder` is a defaulted no-op on the trait.
- **Worker pool (P3):** one shared `Arc<Mutex<TextEmbedding>>` (concurrent embeds serialize on the
  lock — fine for the cheap text model; per-worker handles would 2× RAM, deferred). Backoff
  `1 s·2^attempts` capped 60 s; idle poll 250 ms→2 s. Workers start at `attach_embedder`, independent
  of capture (`02 §5` background trigger); stop-on-exit is best-effort (the startup sweep is the
  real safety net). `embed_text` embeds the whole OCR text as one chunk (`chunk_index = 0`);
  chunking is a non-breaking later addition.
- **`include_chrome` raw-search mechanism (0.2.0 PR2):** `03 §4` left this to PR2 (raw FTS vs
  role-filtered spans FTS). Chose a **dedicated raw FTS5 table** `frame_text_raw_fts` over
  `frame_text.raw_text`: roles aren't populated until PR3 (a spans-FTS would index only `unknown`
  rows now), content==raw in PR2 (a spans-FTS would duplicate the content arm), and a raw FTS is
  stable across PR3 (raw_text is never filtered). `include_chrome=true` adds the raw arm to the
  existing RRF fusion; the content snippet wins on overlap. (`05` PR2.)
- **PR2 span column values + `frame_spans` read (0.2.0 PR2):** spec-silent on the role of a span
  before PR3 classifies — PR2 emits `role=unknown`, `is_searchable=1`, `suppress_reason=NULL`
  (honest "unclassified, kept"; PR3 reclassifies). `filter_version=0` (`UNFILTERED_FILTER_VERSION`),
  `suppressed_count=0`. Added an inherent `SqliteStore::frame_spans` read (mirrors the
  `get_frame`/`delete_frame` observability precedent) so the `text_spans` write path is testable and
  PR3 can read spans to recompute roles. (`05` PR2.)

Manual steps still required (e.g. signing certs, first-run model download, CI secrets):
- **First-run model download** — embedding models (P3) auto-download via fastembed into
  `<app-data>/models/fastembed`. **P4:** the `llama-server` Vulkan binary auto-downloads into
  `<app-data>/sidecar/llama` at launch (off-thread; `sidecar` readiness Initializing→Ready/
  Unavailable), and the vision/answer GGUF (+ mmproj) auto-download via `hf-hub` into
  `<app-data>/models/<lane>/<tier>` **lazily on first request** (gaps #14/#15). Nothing is bundled.
  No download-progress %% yet (readiness is coarse Initializing→Ready); a percentage is a P5 nicety.
- ~~**`onnxruntime.dll` bundling (P3→P5)**~~ — **resolved (v0.1.0):** `fastembed` → `ort` uses the
  `download-binaries` path **without** `load-dynamic` (no `libloading` in `ort-sys`), so ONNX Runtime is
  **static-linked into the app binary** — there is no separate `onnxruntime.dll` to bundle, so the NSIS
  installer needs only the exe. Verified at release time by the v0.1.0 `tauri build` (no DLL emitted
  beside the exe) and the installed-app smoke test (search/embeddings run from the packaged exe alone).
- **Code-signing certificate** for the installer — P5 packaging. Recommended path for this MIT OSS
  project (cheapest → most turnkey): **SignPath Foundation** (free Authenticode signing for
  qualifying OSS) → **Azure Trusted Signing** (~US$10/mo, Microsoft-run, builds SmartScreen
  reputation) → **Certum Open Source Code Signing** (cheap annual cloud cert for OSS devs). Note:
  since 2023 all OV certs require a hardware token or cloud HSM, so a plain file-based cert is no
  longer available; budget for that. Decide owner before P5.
- ~~**Scheduled (0.2.0 PR8) — parallel model download.**~~ **DONE (0.2.0 PR8).** The single-stream
  ~20 MB/s cap is fixed: `fetch_one` now probes for `Range` support and, when present, fetches the
  GGUF/mmproj with N parallel `Range` requests (`DOWNLOAD_CONNECTIONS`, default 8 / env-overridable)
  writing into one pre-allocated `.part` with a per-chunk resume bitmap, reusing the existing
  progress/stall/clean-layout machinery (`crates/inference/src/download.rs`). A range-less server
  falls back to the single-stream hf-hub path; integrity is checked against the LFS sha256 when
  advertised. Owns its tests (parallel byte-identity, resume, fail-fast stall, range-ignored, sha256,
  `range_plan`). The `TODO(0.1.1, perf)` marker was replaced with the PR8 doc comment.
- **TODO (0.1.1) — PR #27 review follow-ups (deferred from v0.1.0; user chose to ship as-is):**
  - *Codex P2 — re-check cache before retrying a locked download.* `download_with_lock_retry`
    (`crates/inference/src/download.rs`) re-calls `download_with_progress` after a `LockAcquisition`
    backoff **without** re-running `fetch_one`'s clean-layout/cache fast paths, so if the lock holder
    finishes during the sleep the loser can miss the now-present GGUF and re-download (or collide on
    publish); the bounded ~20 s backoff can also give up before a real ~4 min 8B download finishes.
    Re-check the destination/finalized blob after each backoff and short-circuit. (Single-instance
    already prevents the main contention, so this is hardening, not a live bug.)
  - *Gemini — single-instance focus.* In the `tauri-plugin-single-instance` callback
    (`src-tauri/src/lib.rs`), call `window.show()` before `unminimize()`/`set_focus()` so a hidden /
    tray-minimized window is restored on a second launch.
- ~~esbuild `allow-scripts` postinstall~~ — **resolved** locally via `npm approve-scripts --all`.
