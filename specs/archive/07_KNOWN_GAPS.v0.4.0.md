# 07 — Known Gaps — archived v0.4.0 history

> Resolved 0.4.0-arc rows (#98/#102/#110/#111/#114/#118/#119), ids preserved, plus the dated PR5/PR6
> gap-audit notes. #102 closed GitHub issue #88 (AI-related recall) at the v0.4.0 release.

| # | Date | Gap (spec was silent on…) | Resolution / decision | Owner | Needed by |
|---|---|---|---|---|---|
| 98 | 2026-07-04 | **#54 improve-AI-tools-recall — closed, folded into the 0.4.0 sessions arc (D7, `docs/0.3.1.md` §2/§4).** Flagging app names / window-title keywords (terminal, Claude Code, …) is exactly the `app_hint` intelligence the 0.4.0 sessions segmentation heuristic needs. | **Building this arc.** `docs/0.4.0.md` now exists; #54's substance becomes the seed **taxonomy** + the segmenter's recognition input (`03 §7e`, D6/D7): patterns are tuned against real captures in **PR2** and shipped in the segmentation engine in **PR4**. Row resolves this arc. **Resolved (v0.4.0): the seed taxonomy + recognition input shipped in the PR4 segmentation engine; #54's app/window-title intelligence is delivered.** | user+agent | resolved (v0.4.0; recognition shipped PR4) |
| 102 | 2026-07-05 | **#88 improve-AI-related-recall — the 0.4.0 sessions arc anchor issue (D11, `docs/0.3.2.md` §2; now `docs/0.4.0.md` §2).** Recognizing Codex / Claude Code / terminal-vs-desktop is the same `app_hint` segmentation input as #98; distinguishing user input from agent output is a **sessions-schema requirement** (a per-artifact role field) — meaningless before session objects exist. | **Building this arc.** Recognition = the taxonomy + segmenter (`03 §7e`, PR2/PR4); user-vs-agent roles = the `session_artifacts` schema (PR3) + best-effort exchange extraction (PR4); surfaces = PR5 (UI) + PR6 (API/MCP). **Closes at PR7.** **Update (PR6, 2026-07-11): the API/MCP surface landed** (read-only `GET /v1/sessions`, `GET /v1/sessions/{id}`, `POST /v1/ask` `session_id` scope, plus MCP `list_sessions`/`get_session`/`ask_session`); the PR5 UI surface already shipped, so both recall surfaces now exist and only the final issue close remains for PR7. **Label-record correction:** the issue was actually carrying **`deferred-0.3.4`** (not the `deferred-0.4.0` this row and `docs/0.4.0.md` line 124 asserted) — 0.4.0 PR1's GitHub hygiene relabels it to the arc's in-progress label + the 0.4.0 milestone. **Resolved (v0.4.0 PR7, 2026-07-11): both recall surfaces shipped (PR5 UI + PR6 API/MCP), proven live at the D16 gate (9 recognized sessions over real history, exchanges with roles); GitHub issue #88 closed at the release.** | user+agent | resolved (v0.4.0 PR7; GitHub #88 closed) |
| 110 | 2026-07-09 | **§7e app-context session key over-segments real fragmented days ~10-40x (PR2 evidence; the arc redesign checkpoint, `docs/0.4.0.md` §3 PR2 hard-stop).** On 2 hand-labeled ground-truth days (07-07, 07-08; timings verified against the live `/v1/export` API, not memory), the candidate segmenter (`resume.rs` generalized, key = app ⊕ domain ⊕ tool) emitted **22 and 44** sessions where the maintainer labeled **5 and 2** task-level sessions. Pooled boundary **F1 0.13** (P 0.07, R 0.75); the full 6x3 gap_close x min_len sweep stays **F1 0.09-0.13, parameter-independent**. `replay` shows one session per sustained app-run: a task worked across Codex + browser + terminal shatters into a session per app, because the key splits on every sustained app switch. | **Flag-and-gather (maintainer decision 2026-07-09); do NOT freeze PR3 / schema 11 on this.** The evidence says the app-context key likely cannot yield useful task-level flow-recall sessions as `§7e` specifies; a task-level grouping (merge adjacent recognized-tool runs, absorb focus/browser excursions of any length) is the probable redesign, but 2 unusually-fragmented days plus possibly-coarse labels are too thin to decide it. Accumulate more (incl. calmer) days, re-score, then decide: revise the `§7e` close/merge rule (a `06` contract amendment) before PR3, or confirm the key. PR2 stays paused short of the D9 thresholds and the `06` #26 gate. **Update (2026-07-10): redesign call made.** The maintainer confirmed every real day is this fragmented ("no calm days"), so the gather-to-decide rationale is void and the direction is settled: the app-context key becomes a **task-level anchored grouping** (`06` #27, approved in direction). The redesign + its validation are now the active PR2 work; this row flips to **resolved-by-#27** at Phase C once the harness rerun numbers land. **Resolved (PR2b Phase C, 2026-07-10) by `06` #27/#28:** the app-context key became the identity-anchored **concurrent** grouping. On the held-out 07-09 the concurrent model scores partitioned typed boundary F1 **0.286** (vs the app-key micro baseline **0.068**) with perfect tool recognition — the over-segmentation is resolved; the residual low absolute F1 is capture-capped (`07` #117), not a key-granularity problem. | user+agent | resolved 2026-07-10 (`06` #27/#28) |
| 111 | 2026-07-09 | **Claude-Code-in-terminal title recognition is weak: the "claude" pattern misses task-titled frames (PR2 evidence; feeds #109's "too weak" clause).** Claude Code writes the WindowsTerminal title as the current task with a spinner/sparkle prefix (`✳` / braille `U+2800-28FF`), e.g. "✳ Post review comments on PR", only sometimes the literal "Claude Code". The seed `claude-code` title pattern `["claude"]` catches roughly a quarter to a third of real Claude-Code terminal frames on the ground-truth days; the spinner prefix is the reliable marker (it flagged 61/77 and 22/24 frames). Tool recognition on the 2 labeled days is 0.40 after the codex-vs-vscode fix, held down mainly by these missed claude-code frames. | **Resolving via taxonomy v3 (PR2 redesign, 2026-07-09/10).** Confirmed on the labeled days: **every** terminal frame containing the substring "claude" is also spinner-prefixed (zero claude-without-spinner), and switching to a spinner-prefix rule raises terminal-frame coverage 19/101 → 83/101 (82 %); one entire labeled AI session (8/8 terminal frames spinner-marked, 0/8 substring-marked) goes from fully missed to fully recognized. Built as a matcher extension: a new `title_prefix_ranges` taxonomy field (`03 §7e` regex-upgrade path) that matches the first non-whitespace char's Unicode scalar against `U+2733` + `U+2800..=U+28FF`, **ANDed to terminal stems**, with the `"claude"` substring kept as a costless fallback. Specificity caveat (another TUI could spinner-prefix its title) is bounded by the terminal-stem AND + entry ordering and recorded in #112; re-verify on every new labeled day. **Resolved (v0.4.0): the `title_prefix_ranges` spinner-prefix matcher (taxonomy v3) shipped in the PR4 engine and is exercised live (the D16 backfill recognized `ai:claude-code` at 0.95 confidence).** | agent | resolved (v0.4.0; taxonomy v3 shipped PR4) |
| 114 | 2026-07-10 | **Session concurrency: the serial `§7e` model may not fit a user running multiple recognized tools at once (PR2 finding; possible arc-level fork).** PR2 evidence showed that more than one recognized AI tool can be active **in the same time window** (overlapping tool activity, not strictly one-at-a-time). The current design emits **one non-overlapping session per instant** (segmenter `debug_assert!` non-overlap; `labels.toml` validates non-overlapping; `03 §7e`/`§4` describe serial sessions), so concurrent tool use collapses into a single band. This is deeper than #110 (app-vs-task granularity): it is serial-vs-concurrent. | **Resolved (maintainer, 2026-07-10): option (b) — concurrent, built in PR2b (`06` #28).** Sessions of **different identities may overlap in wall-clock time** (an AI track can run through a meeting; two AI tools can be active at once); the serial redesign is kept as the `--algo grouped` A/B baseline. **Exclusive frame ownership is preserved:** a frame has one foreground `app_hint` → one micro-span → one identity track → **exactly one session**, so sessions overlap only in span, never in frame ownership. This is what makes the change fit **schema 11 with zero DDL delta** (verified against `03 §4`: `sessions` has no exclusion/non-overlap constraint, `idx_sessions_time(started_at, ended_at)` is overlap-agnostic, and `frames.session_id` is a single nullable FK — an overlapping-session set satisfies every schema-11 CHECK) — so **PR3 does not change** because of concurrency. Segmenter becomes a **per-identity-track walk** (a map track_key → open group replacing the single open group; per-track non-overlap `debug_assert!`, global overlap allowed); `labels.toml` becomes **v2** (per-identity non-overlap: `ai` grouped by tool, `focus`/`other` pooled per kind, `meeting` labels may overlap each other — backward-compatible with serial label files); the referee's primary metric becomes **identity-partitioned** typed boundary F1 (pooled position-only kept as the comparability line). Concurrency also **structurally resolves the #112(b) host-precedence pathology** (a desktop AI app out-framing its own terminal was two concurrent identities collapsed into one group; now two tracks). Accepted concurrency limitations → new #116. Numbers + commit recorded at PR2b Phase C (the D9 gate, `06` #26), same as #27/#28. | user+agent | resolved 2026-07-10 (built in PR2b; numbers land at Phase C) |
| 118 | 2026-07-11 | **`GET /v1/sessions` limit bound was unspecified (PR6).** `03 §7c` listed the `limit=` query param for the sessions list surface but pinned neither a default nor a clamp, unlike the search surface. | **Maintainer-decided (2026-07-11):** `limit` **defaults to 1000** and is **clamped to 1..=1000**, mirroring the in-app `list_sessions` IPC surface so the API and the app return the same bounded page. Home: constants `SESSIONS_LIMIT_DEFAULT`/`SESSIONS_LIMIT_MAX` in `crates/api/src/sessions.rs`; now pinned in `03 §7c` (the GET `/v1/sessions` bullet). Additive, read-only (D12); no schema/settings change. | user+agent | resolved (PR6) |
| 119 | 2026-07-11 | **Summary visibility over the read-only API/MCP was underspecified (PR6).** `03 §7c` said `include_summary=1` returns the cached summary or null and never generates, but did not state what the **list** rows and a **detail-without-the-flag** request serve for `summary`/`summary_model`, nor that `title` is unconditional. | **Maintainer-decided (2026-07-11):** list rows and detail-without-`include_summary` serve `summary`/`summary_model` as **null**; `title` is **always** included; `include_summary=1` reveals the **cached** value only and **never** generates (the generating path stays in-app IPC, `06` #32, D12). Home: `api_session`'s `reveal_summary` logic in `crates/api/src/sessions.rs`; now pinned in `03 §7c` (the GET `/v1/sessions/{id}` bullet). MCP `get_session` inherits the boundary. Additive, read-only (D12). | user+agent | resolved (PR6) |

### 0.4.0 PR5 Task 1 gap audit — 2026-07-10

No new silent-spec gaps were opened. The remaining live acceptance work is already contracted in
`03 §13c-5`: the integrated frontend/live-app drill-in and real-frame Recap check belong to the rest
of PR5, while API/MCP session exposure remains PR6-owned.

### 0.4.0 PR5 Task 2 gap audit — 2026-07-10

No new silent-spec gaps were opened. Static UI gates cover Hooks, typing, code splitting, and the
production bundle; the already-contracted `03 §13c-5` live acceptance remains: run the native app on
real session data and verify band → drill-in → Moment → back, cited Recap, and the fixed-four-lane
session-ribbon/single-scroll layout at supported sizes and DPI. API/MCP exposure remains PR6-owned.

### 0.4.0 PR5 final gap audit — 2026-07-10

No new silent-spec gap was opened. The final review fixes stay inside the existing session-UI and
shell-layout contracts, and PR5 makes no schema/migration change. `docs/TESTING.md` now states the
native/manual acceptance procedure and observed evidence precisely. The real `npm run dev`
Tauri/WebView2 run passed band navigation, exact Session↔Moment return, cited Recap ownership and
cancellation, low-confidence/no-exchange honesty, keyboard operation, reduced motion, Settings
clamps, and the supported layout matrix (`05` Pass 9). The live dataset contained 21 sessions and
zero open rows, so the open-session variant is recorded as **unavailable**, not passed or failed.
That absence creates no new product ambiguity or known gap; implementation/test coverage remains.

### 0.4.0 PR5 final-review decision audit — 2026-07-11

No new silent-spec gap remains after the dense-layout ambiguity was raised explicitly and the user
selected option 1: **exactly four lanes in every state; lane 5+ aggregated into a neutral keyboard
control that focuses existing range presets; D9 no-CLS is not relaxed** (`06` #31, `05` Pass 10).
The typed-null `sessions_changed` event is pull-based mounted-query invalidation after successful
scheduler work, not a notification/toast. No schema/version, API/MCP, audio, nudge, score, NavRail,
or frame-level behavior change was introduced.

### 0.4.0 PR5 PR #104 review gap audit — 2026-07-11

No new known gap or silent-spec ambiguity was opened. All four unresolved review threads were
applicable and resolved in `c360d7e`; the two store threads were duplicates. Exact Rust Unicode trim
semantics remain intact in the bounded SQL preflight, Timeline error/empty geometry now honors the
existing fixed-four-lane decision, and scheduler invalidation now distinguishes semantic changes
from unchanged passes while using delta writes. No schema/migration, API/MCP, audio, notification,
nudge, score, NavRail, or frame-level behavior change was introduced.
