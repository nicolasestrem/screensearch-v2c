# 06 — Patch Plan

> **Ordered fixes required before shipping**, plus any **spec contradictions** found during the
> build (`04 §5`). Empty until the build surfaces something.

| # | Priority | Issue | Source (spec §, file:line) | Fix | Status |
|---|---|---|---|---|---|
| 1 | — | **Vision scheduling model changed (user decision 2026-06-21).** `03 §8` modelled vision triggering as a single `enrich.vision_mode` enum (`on_demand`/`timer`/`idle`). Per the user, timed and idle enrichment must each be an **independent opt-in toggle (off by default) + a user-set threshold**; on-demand is always available. | `03 §8`; `crates/traits/src/ipc.rs` (`Settings`) | Replaced `enrich_vision_mode: VisionMode` with `enrich_vision_timer_enabled`/`_interval_ms` + `enrich_vision_idle_enabled`/`_idle_secs`; removed the `VisionMode` enum. **`03 §8` updated to match.** | ✅ applied |
| 2 | — | **OCR confidence not available from the chosen engine (P2).** `03 §3` `OcrResult.mean_confidence: f32` (and `ocr_text.mean_confidence REAL NOT NULL`) assume a per-word/line confidence, but the spec-mandated WinRT `Media.Ocr` exposes **none** — `OcrWord` has only `Text` + `BoundingRect` (verified against Microsoft Learn). | `03 §3/§4`; `crates/ocr/src/lib.rs` | **Resolved (user, 2026-06-21):** record the sentinel **`-1.0` = "unknown"** (`ocr::CONFIDENCE_UNKNOWN`) rather than fabricate a score; no schema/trait change. The P5 UI renders negative as "n/a". (See `07`.) | ✅ applied |
| 3 | P2 | **PR #21 audit-doc review follow-up.** Reviewers found the audit artifact had not mirrored the live follow-up work into `05_BUILD_REVIEW.md` / `07_KNOWN_GAPS.md`, and the report overlabelled local Node 26 frontend checks as CI-equivalent despite CI using Node 22. | PR #21 review threads; `AGENTS.md`; `04 §7`; `.github/workflows/ci.yml` | Mirrored the audit follow-up into `05`, added open gaps #40–#45 to `07`, clarified the audit as CI-order local smoke rather than exact CI-runtime evidence, and noted that the obsolete-term search was captured before the audit artifact existed. | ✅ applied |
| 4 | P2 | **Vision scheduler reverses two `07` #19 decisions (user-reported, 2026-06-24).** (a) The timer/idle batch size was hardcoded `const BATCH = 20` — not configurable, so logs showed a fixed `count=20`. (b) `07` #19 had accepted "**no dedup** of pending `vision_tag` jobs (re-enqueue is harmless — `insert_vision` is an idempotent upsert)", but with the batch re-queued every tick this churns the queue and double-queues across the timer/idle lanes. | `07` gap #19; `crates/kernel/src/vision_scheduler.rs`; `crates/store/src/records.rs` (`untagged_frame_ids`) | Added the `enrich.vision_batch_size` setting (default 20, clamp 1–500) read each scheduled run; added a `NOT EXISTS` pending/running `vision_tag` guard to `untagged_frame_ids` so in-flight frames aren't re-enqueued. The upsert stays as the data backstop. `07` #19 updated; no schema/IPC/trait-signature change (`Settings` gains one field → bindings regenerated). **PR #23 review hardening:** also exclude `dead` jobs (no infinite retry of poisoned frames — Gemini); forward migration v2 adds `idx_jobs_frame_kind_state` for the dedup lookup (Gemini/Claude); the timer/idle producers share a mutex across read-then-enqueue for an atomic select-and-insert (Codex); documented the new key in `03` §8 (Codex). | ✅ applied |
| 5 | P6 | **Report context must scale with the time range, not run a flat single window (user decision 2026-06-25).** `03 §8b` modelled `generate_report` as a map-reduce over a relevance/recency selection; the user rejected a flat 8192-token window for a weekly report as "ridiculous" and required context to **scale with the range and guarantee temporal coverage** (every active day represented, a dense Monday must not starve a quiet Saturday). | `03 §8b`; `docs/0.2.0.md` PR6; `crates/kernel/src/reports.rs` | **Calendar-Grid Coverage Map-Reduce (CGCMR), applied.** Deviations from the literal `§8b` text, all covered by the user directive: **(a)** batching is a fixed **per-calendar-day grid** (one **or more** MAP passes per active period), not §8b token-budget batches — to *structurally* guarantee per-day coverage. A period holding more content than one 8192 window is split into pass-sized sub-batches before the MAP step (combining the grid with §8b-style token batching *within* a period), so a dense day — or a single-period Daily range, which is one group — fans out into several passes and the REDUCE folds them back rather than being truncated to one window. Within-period coverage is therefore complete, not merely per-period (added in the PR #33 review; the collapse-to-single-pass fast path now fires only when the whole report genuinely fits one window). **(b)** Daily/Weekly/Custom-without-prompt use **even ASC temporal striding** (`Store::sample_frames_in_range`) + chronological order, not best-first relevance; **Custom *with* a prompt** drives `hybrid_search(prompt, time_range)` (relevance) per the confirmed decision. **(c)** REDUCE may recurse **>1 level** for long ranges (consecutive `REDUCE_FANOUT`=6 groups, time order preserved), bounded by `MAX_REPORT_PASSES`=64. **(d)** the four settings keys are reinterpreted: `reports.daily_top_k` = **per-active-period frame budget**, `reports.weekly_top_k` = **global frame cap**; structural bounds (`MIN_FRAMES_PER_PERIOD`, `REDUCE_FANOUT`, `MAX_PERIODS`, `MAX_REPORT_PASSES`, `MAX_REPORT_CITATIONS`) are named constants (gap #60). **(e)** `report_progress` events + `cancel_report` are **additive** to the `generate_report -> ReportResponse` signature (the command still returns the value). **(f)** `n_ctx` stays **flat 8192** (the ctx-boost alternative was rejected: zero coverage benefit, forces a sidecar relaunch, no iGPU VRAM probe) — the *number* of 8192-bounded passes grows with the range, not the window. Honest-empty ranges issue **zero** sidecar calls. | ✅ applied |

When the spec contradicts itself, stop, ask the user, and log the resolution here before coding.
