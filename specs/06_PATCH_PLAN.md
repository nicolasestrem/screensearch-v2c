# 06 — Patch Plan

> **Ordered fixes required before shipping**, plus any **spec contradictions** found during the
> build (`04 §5`). Empty until the build surfaces something.

| # | Priority | Issue | Source (spec §, file:line) | Fix | Status |
|---|---|---|---|---|---|
| 15 | P5 | **Upstream llama.cpp multimodal host-memory leak (build 9842).** ~149 MB committed host RAM leaked per vision inference; VRAM flat. Mitigated locally by the `sidecar.recycle_*` valve (recycle at a committed-RAM ceiling). Real fix requires a newer bundled `llama-server` build. | `07` #72; `specs/03_MASTER_PRODUCTION_SPEC.md §8`; `crates/inference` sidecar lifecycle | **Open (upstream).** Local mitigation shipped (`sidecar.recycle_enabled` default true, `sidecar.recycle_rss_mb` default 0=auto). Upgrade the bundled llama-server build when a fix lands upstream; re-run the 60-frame isolated test to confirm RSS stays flat. | open (upstream) |
| 23 | P3 | **PDH `\GPU Engine(*)\Utilization Percentage` is blind to the sidecar's Vulkan compute** on this NVIDIA box: ~1–2 % summed across all engines while `nvidia-smi` reads 80–100 % during vision inference. `crates/sysmon`'s GPU pressure probe uses exactly these counters, so the 0.2.1 enrichment throttle's GPU arm (`throttle.gpu_enter_pct`) can never engage for the workload it was built to throttle — `gpu_monitored=true` yet effectively unmonitored. CPU arm unaffected. Found while profiling `05` Pass 3 (v0.3.1 archive). | `crates/sysmon` (PDH probe); `03 §5` (throttle contract) | Not a 0.3.1 item (no new subsystems — D8). Future arc: NVML-based probe or per-`engtype` counter audit on Vulkan workloads. | open |
| 26 | P1 | **(RESERVED) D9 segmentation acceptance thresholds — the PR4 merge gate.** Placeholder id; the binding numbers land here at PR2 Phase C after the redesign (#27) is validated on labeled + fresh days and the maintainer approves them. | `docs/0.4.0.md` §3 PR2/§13c-2; `07` #26-manual (n/a) | **SET — maintainer-approved (PR2b Phase C, 2026-07-10).** The binding PR4 merge gate, **recognition-primary with a boundary-F1 floor**: boundary F1 is structurally capped by foreground-only capture (`07` #117), so it is a floor, not the target — recognition is the arc's payoff. PR4's shipped segmenter, re-run through the harness referee (`--algo concurrent`, identity-partitioned metric, labels snapped, 120 s + 180 s) on the tuning days (07-07/08) **plus the held-out 07-09** (07-10 is excluded — the concurrency-capture-limit demonstrator, `07` #117), must meet ALL of: **PRIMARY** — (a) **tool accuracy ≥ 0.65**; (b) **predicted session count within 2×** of labeled per day; (c) **beats both baselines** (`--algo micro` and `--algo grouped`) on the same days. **FLOOR** — (d) **partitioned typed boundary F1 ≥ 0.20 at ±120 s** (pooled over the scored days). **Freeze window W = 24 h** (`03 §7e`; stability was 6 h-stable on the tuning days, so 24 h holds with wide margin). **Achieved** at the frozen params (`merge_gap 2700 s`, `absorb_max 1800 s`, rest at defaults): tuning pooled partitioned F1 **0.452 / 0.581** at 120/180 s (posF1 0.581/0.645), tool **0.714**; **held-out 07-09** partitioned F1 **0.286**, posF1 0.286, recall 0.25, tool **1.000**, 4 vs 5 sessions — vs `micro` **0.068** and `grouped` **0.167** (concurrent wins 2–4×). A miss is a stop-and-report, not a tune-until-green (D9). | gate SET (PR2b Phase C) |
| 27 | P1 | **§7e session context key + close rule contradicted by PR2 ground truth (gap #110) — the task-level grouping amendment.** Evidence (aggregate, harness referee): the per-app context key emits 22/44 candidate sessions on two hand-labeled days where the maintainer labeled 5/2 task-level sessions; pooled typed boundary F1 0.128 at ±120 s, **parameter-independent** across the entire gap_close × min_len sweep (0.092–0.136). Labeled sessions contain 34–351 app-runs each and the labeled tool's process stem is often not even frame-dominant, so no key carrying the raw app term can reproduce them. The single decisive rule (absorbing short different-identity runs instead of closing on them) alone moved a two-level feasibility probe from F1 0.200 → 0.571 with exactly-right session counts. | `03 §7e` (context key / close / excursion / incremental / freeze sentences); `03 §4` (`sessions.context_key` comment, the sanctioned escape hatch); `docs/0.4.0.md` §3 PR2 hard-stop; supersedes the #110 flag-and-gather pause | **Amendment approved in direction (maintainer, 2026-07-09/10); numbers + commit recorded at resolution (Phase C).** Sessions become **task-level anchored groups** via a two-pass segmenter (micro app-runs → macro identity-anchored groups). §7e sentence deltas: [a] the context-key sentence splits into two levels — the §7b-generalized key stays the internal **micro-run** key (where-was-i/`resume.rs` untouched, D10), while `sessions.context_key` stores the closed task-level grammar `"ai:<tool-id>" \| "meeting:<taxonomy-id>" \| "focus:<dominant-app-stem>"`; [b] a session closes on inactivity ≥ `sessions.merge_gap_secs`, on a foreign **recognized identity** sustained beyond `sessions.absorb_max_secs`, or at a meeting-band edge (`gap_close_secs`/`min_len_secs` keep their pass-1 micro roles); [c] the transient-excursion sentence generalizes contexts → identities (foreign app-runs and different-identity AI runs up to `absorb_max` absorb; meeting identity never absorbs — a hard open/close band at presence endpoints, chained at `sessions.meeting_gap_secs`, owning its interior frames); [d] the incremental clause assigns new frames by **anchor continuity** (the accretion rule); [e] new sentence — anchorless (focus) sessions additionally require `sessions.focus_min_len_secs` and the `sessions.focus_min_density_fph` gate; suppressed activity stays sessionless (D10) and anchorless sessions carry lower confidence (D11); [f] kind/tool/context_key freeze at the same instant as boundaries. **Zero DDL change** — macro rows satisfy every schema-11 CHECK; PR3 only re-normalizes the `§4` context_key column comment via the escape hatch. New named non-settings constants (attribution correctness): `IDENTITY_QUALIFY_MS` (anchor-qualification threshold, splits `min_len`'s former triple duty) and `HOST_PRECEDENCE` (terminal > ide > desktop > browser). Accepted limitations recorded in `07` #112/#113. **Resolved (PR2b Phase C, 2026-07-10):** the serial two-pass segmenter is built + validated as the `--algo grouped` A/B baseline (held-out 07-09 partitioned F1 0.167, tuning 0.375/0.438); the **concurrent** model (#28) superseded it as the shipped/default path after `07` #114 resolved concurrent. `#110` → **resolved-by-#27/#28** (the app-context key became the identity-anchored concurrent grouping). | amendment (resolved PR2b Phase C; baseline retained) |
| 28 | P1 | **Concurrent session model (`07` #114 resolved) — the per-identity-track amendment layered on #27.** PR2's fresh-day labeling showed real usage runs **parallel** recognized tools (an AI agent through a meeting; two AI tools at once). #27's serial two-pass segmenter — one open group, a non-overlapping `debug_assert!`, non-overlapping `labels.toml`, and the "one session per instant" framing — collapses concurrent identities into a single precedence-attributed band, so it cannot represent the ground truth. Maintainer resolved `07` #114 to the **concurrent** option (2026-07-10). | `03 §7e` (the #27-amended close/excursion/freeze sentences, further generalized here); `03 §4` (verified: **no** DDL change needed); `06` #27 (the serial base this layers on); `07` #114; `docs/0.4.0.md` §3 PR2 | **Amendment approved (maintainer, 2026-07-10); numbers + commit at PR2b Phase C.** Sessions become **per-identity concurrent tracks**. Deltas over #27: **track key** = `ai:<tool>` per taxonomy id, `meeting:<id>` per meeting id, plus **one residual `focus` track** (not per-stem — that would resurrect #110 fragmentation); same-track non-overlap enforced, cross-track overlap allowed. **Exclusive frame ownership** (one `app_hint` → one micro-span → one track → one session) keeps **zero DDL change** — an overlapping-session set satisfies every schema-11 CHECK (`03 §4`), so **PR3 is unaffected** by concurrency. The single-open-group walk becomes a **map track_key → open group**; #27's anchor **selection** and `HOST_PRECEDENCE` become inert on the shipped path (each id is its own track — kept only in the `--algo grouped` serial baseline), while anchor **qualification survives** (an AI track emits only if its summed own-recognized presence ≥ `IDENTITY_QUALIFY_MS`, since the span floor alone passes hollow chains); the `SustainedForeignIdentity` close **disappears** (a foreign recognized run opens **its own** track, never closes the incumbent). §7e sentence deltas layered on #27: [b] a track closes only on **its own** inactivity ≥ `merge_gap_secs` or at end of input (foreign-identity close removed); [c] transient absorption becomes a **per-track None-budget**: an unrecognized run ≤ `absorb_max_secs` absorbs into the **last-touched open AI track** (the exact per-track transcription of #27's validated absorb-budget that moved F1 0.200→0.571), a leading run ramps into an opening track (convert-not-split preserved), else it is focus material; a run > `absorb_max_secs` is focus; **meetings still never absorb**, but meeting bands are **no longer walk barriers** (an AI track spans through a meeting) and overlapping meetings emit overlapping sessions (`merge_overlapping_bands` dropped on this path). Floors stay per-track (density gate remains focus-only — background AI tracks are low-density by construction); **zero new settings knobs** (`absorb_max` reused as the None budget, `merge_gap` goes per-track). `labels.toml` → **v2** (per-identity non-overlap: `ai` by tool, `focus`/`other` pooled per kind, `meeting` labels may overlap; backward-compatible with serial files). Referee primary metric → **identity-partitioned** typed boundary F1 (partitions `ai:<tool>`/`meeting`/`focus`/`other`; strictly ≤ the pooled position-only F1 by construction, so the cross-identity penalty is built in; pooled kept as the 0.128/0.50 comparability line). Serial #27 redesign retained as the `--algo grouped` A/B baseline. Accepted limitations → `07` #116. **Resolved (PR2b Phase C, 2026-07-10):** built in `crates/harness/src/group.rs` (`group_concurrent`) + `score.rs` (identity-partitioned referee) + `main.rs` (`--algo concurrent`, now the default); validated on the tuning days (07-07/08) and the held-out 07-09. Frozen params `merge_gap 2700 s` / `absorb_max 1800 s` (the sweep's tuning-day optimum; a clear local F1 peak); `meeting_gap` + `focus_min_density` demoted to constants (1-D flat), the other knobs kept at defaults (their 1-D optima diverge on n=2 — greedy combination would overfit). Numbers + the D9 gate → `06` #26. **The concurrent model beats both baselines on the held-out day** (partitioned F1 0.286 vs grouped 0.167 vs micro 0.068) with perfect tool recognition (1.000); the low absolute boundary F1 is capture-capped (`07` #117), which is why the gate is recognition-primary. | amendment (resolved PR2b Phase C) |

> Resolved v0.1.0-era rows (#1–#4) → `specs/archive/06_PATCH_PLAN.v0.1.0.md` (ids preserved).
> Shipped 0.2.x rows (#5–#14 + the 0.2.1 build notes) → `specs/archive/06_PATCH_PLAN.v0.2.x.md` (ids preserved).
> Resolved 0.3.0-arc rows (#16–#20) → `specs/archive/06_PATCH_PLAN.v0.3.0.md` (ids preserved).
> Resolved 0.3.1-era rows (#21/#22/#24) → `specs/archive/06_PATCH_PLAN.v0.3.1.md` (ids preserved).
> 0.3.2 opened no rows → `specs/archive/06_PATCH_PLAN.v0.3.2.md` (stub, for per-release symmetry).
> Resolved 0.3.3 row (#25 — UIA skips Chromium/Electron) → `specs/archive/06_PATCH_PLAN.v0.3.3.md` (id preserved).
> Live rows: #15 (upstream llama.cpp leak), #23 (PDH blind to Vulkan), #26 (**D9 gate SET** at PR2b Phase C — the binding PR4 merge gate), #27 (serial §7e grouping — **resolved** PR2b Phase C, retained as the A/B baseline), #28 (concurrent session model — **resolved** PR2b Phase C, the shipped/default path).

When the spec contradicts itself, stop, ask the user, and log the resolution here before coding.
