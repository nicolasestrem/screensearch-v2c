# 06 — Patch Plan

> **Ordered fixes required before shipping**, plus any **spec contradictions** found during the
> build (`04 §5`). Empty until the build surfaces something.

| # | Priority | Issue | Source (spec §, file:line) | Fix | Status |
|---|---|---|---|---|---|
| 15 | P5 | **Upstream llama.cpp multimodal host-memory leak (build 9842).** ~149 MB committed host RAM leaked per vision inference; VRAM flat. Mitigated locally by the `sidecar.recycle_*` valve (recycle at a committed-RAM ceiling). Real fix requires a newer bundled `llama-server` build. | `07` #72; `specs/03_MASTER_PRODUCTION_SPEC.md §8`; `crates/inference` sidecar lifecycle | **Open (upstream).** Local mitigation shipped (`sidecar.recycle_enabled` default true, `sidecar.recycle_rss_mb` default 0=auto). Upgrade the bundled llama-server build when a fix lands upstream; re-run the 60-frame isolated test to confirm RSS stays flat. | open (upstream) |
| 23 | P3 | **PDH `\GPU Engine(*)\Utilization Percentage` is blind to the sidecar's Vulkan compute** on this NVIDIA box: ~1–2 % summed across all engines while `nvidia-smi` reads 80–100 % during vision inference. `crates/sysmon`'s GPU pressure probe uses exactly these counters, so the 0.2.1 enrichment throttle's GPU arm (`throttle.gpu_enter_pct`) can never engage for the workload it was built to throttle — `gpu_monitored=true` yet effectively unmonitored. CPU arm unaffected. Found while profiling `05` Pass 3 (v0.3.1 archive). | `crates/sysmon` (PDH probe); `03 §5` (throttle contract) | Not a 0.3.1 item (no new subsystems — D8). Future arc: NVML-based probe or per-`engtype` counter audit on Vulkan workloads. | open |
| 26 | P1 | **(RESERVED) D9 segmentation acceptance thresholds — the PR4 merge gate.** Placeholder id; the binding numbers land here at PR2 Phase C after the redesign (#27) is validated on labeled + fresh days and the maintainer approves them. | `docs/0.4.0.md` §3 PR2/§13c-2; `07` #26-manual (n/a) | **Open — DEFERRED past this PR (maintainer, 2026-07-10).** Setting the gate is blocked not on data but on the session **model** being unsettled: `07` #114 (concurrency) surfaced that real usage runs parallel AI agents, which the serial segmenter cannot represent, so scoring it against any labels validates against a moving target. The gate is set only **after** the serial-vs-concurrent decision (#114). Proposal shape when it lands (margin below achieved): pooled typed boundary F1 ≥ 0.55 AND recall ≥ 0.60 at ±120 s, tool accuracy ≥ 0.7, session count within 2× of labeled, on a set including ≥1 fresh day not used for tuning; ±180 s secondary. Preliminary in-sample grouped result at defaults: F1 0.50/0.57 vs baseline 0.14/0.16 (`05` Pass 2, NOT the gate). | gate (deferred until #114 decided) |
| 27 | P1 | **§7e session context key + close rule contradicted by PR2 ground truth (gap #110) — the task-level grouping amendment.** Evidence (aggregate, harness referee): the per-app context key emits 22/44 candidate sessions on two hand-labeled days where the maintainer labeled 5/2 task-level sessions; pooled typed boundary F1 0.128 at ±120 s, **parameter-independent** across the entire gap_close × min_len sweep (0.092–0.136). Labeled sessions contain 34–351 app-runs each and the labeled tool's process stem is often not even frame-dominant, so no key carrying the raw app term can reproduce them. The single decisive rule (absorbing short different-identity runs instead of closing on them) alone moved a two-level feasibility probe from F1 0.200 → 0.571 with exactly-right session counts. | `03 §7e` (context key / close / excursion / incremental / freeze sentences); `03 §4` (`sessions.context_key` comment, the sanctioned escape hatch); `docs/0.4.0.md` §3 PR2 hard-stop; supersedes the #110 flag-and-gather pause | **Amendment approved in direction (maintainer, 2026-07-09/10); numbers + commit recorded at resolution (Phase C).** Sessions become **task-level anchored groups** via a two-pass segmenter (micro app-runs → macro identity-anchored groups). §7e sentence deltas: [a] the context-key sentence splits into two levels — the §7b-generalized key stays the internal **micro-run** key (where-was-i/`resume.rs` untouched, D10), while `sessions.context_key` stores the closed task-level grammar `"ai:<tool-id>" \| "meeting:<taxonomy-id>" \| "focus:<dominant-app-stem>"`; [b] a session closes on inactivity ≥ `sessions.merge_gap_secs`, on a foreign **recognized identity** sustained beyond `sessions.absorb_max_secs`, or at a meeting-band edge (`gap_close_secs`/`min_len_secs` keep their pass-1 micro roles); [c] the transient-excursion sentence generalizes contexts → identities (foreign app-runs and different-identity AI runs up to `absorb_max` absorb; meeting identity never absorbs — a hard open/close band at presence endpoints, chained at `sessions.meeting_gap_secs`, owning its interior frames); [d] the incremental clause assigns new frames by **anchor continuity** (the accretion rule); [e] new sentence — anchorless (focus) sessions additionally require `sessions.focus_min_len_secs` and the `sessions.focus_min_density_fph` gate; suppressed activity stays sessionless (D10) and anchorless sessions carry lower confidence (D11); [f] kind/tool/context_key freeze at the same instant as boundaries. **Zero DDL change** — macro rows satisfy every schema-11 CHECK; PR3 only re-normalizes the `§4` context_key column comment via the escape hatch. New named non-settings constants (attribution correctness): `IDENTITY_QUALIFY_MS` (anchor-qualification threshold, splits `min_len`'s former triple duty) and `HOST_PRECEDENCE` (terminal > ide > desktop > browser). Accepted limitations recorded in `07` #112/#113. Resolves at Phase C with the harness rerun numbers + commit hash; #110 flips to resolved-by-#27 then. | amendment (open until Phase C) |

> Resolved v0.1.0-era rows (#1–#4) → `specs/archive/06_PATCH_PLAN.v0.1.0.md` (ids preserved).
> Shipped 0.2.x rows (#5–#14 + the 0.2.1 build notes) → `specs/archive/06_PATCH_PLAN.v0.2.x.md` (ids preserved).
> Resolved 0.3.0-arc rows (#16–#20) → `specs/archive/06_PATCH_PLAN.v0.3.0.md` (ids preserved).
> Resolved 0.3.1-era rows (#21/#22/#24) → `specs/archive/06_PATCH_PLAN.v0.3.1.md` (ids preserved).
> 0.3.2 opened no rows → `specs/archive/06_PATCH_PLAN.v0.3.2.md` (stub, for per-release symmetry).
> Resolved 0.3.3 row (#25 — UIA skips Chromium/Electron) → `specs/archive/06_PATCH_PLAN.v0.3.3.md` (id preserved).
> Live rows: #15 (upstream llama.cpp leak), #23 (PDH blind to Vulkan), #26 (D9 gate, reserved → populated at 0.4.0 PR2 Phase C), #27 (§7e task-level grouping amendment, open until Phase C).

When the spec contradicts itself, stop, ask the user, and log the resolution here before coding.
