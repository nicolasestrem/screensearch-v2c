# 06 — Patch Plan

> **Ordered fixes required before shipping**, plus any **spec contradictions** found during the
> build (`04 §5`). Empty until the build surfaces something.

| # | Priority | Issue | Source (spec §, file:line) | Fix | Status |
|---|---|---|---|---|---|
| 15 | P5 | **Upstream llama.cpp multimodal host-memory leak (build 9842).** ~149 MB committed host RAM leaked per vision inference; VRAM flat. Mitigated locally by the `sidecar.recycle_*` valve (recycle at a committed-RAM ceiling). Real fix requires a newer bundled `llama-server` build. | `07` #72; `specs/03_MASTER_PRODUCTION_SPEC.md §8`; `crates/inference` sidecar lifecycle | **Open (upstream).** Local mitigation shipped (`sidecar.recycle_enabled` default true, `sidecar.recycle_rss_mb` default 0=auto). Upgrade the bundled llama-server build when a fix lands upstream; re-run the 60-frame isolated test to confirm RSS stays flat. | open (upstream) |
| 16 | P5 | **Spec demanded an Inno Setup installer + portable ZIP, but Tauri 2 ships NSIS/MSI natively; v0.1.0 shipped NSIS only** — a standing spec-vs-reality contradiction (`07` #26). | `00 §G`, `01`, `02 §5`, `03 §11`+§13.9, `docs/ARCHITECTURE.md §12`, `.github/workflows/ci.yml`, `README.md` | **✅ Resolved (user-approved full sweep, `chore/cancel-inno-and-a11y-matrix`):** rewrote all 9 live refs to **NSIS**; Inno/MSI/portable ZIP **dropped**; DoD §13.9 re-scoped to NSIS and **met**; `07` #26 closed. Code-signing stays the lone open packaging item (`07` "Manual steps"). | ✅ resolved |
| 17 | P5 | **Stale `02 §4` diagram caption after the image-lane removal.** The architecture ASCII diagram read `• fastembed (text+image vectors, in-process)`, but PR1 already rewrote the contract sections (`00`/`03`/`MODEL_REGISTRY`) to "text is the only embedding lane". A diagram caption, not a contract clause — cosmetic drift, not a stop-worthy contradiction. | `specs/02_STRATEGIC_PLAN.md:61` | **✅ Fixed in PR4** (`feat/pr4-image-lane-removal`): caption → `• fastembed (text vectors, in-process)`, matching the post-PR4 contract. Found while sweeping specs for image-lane references. | ✅ resolved |
| 18 | P4 | **`idx_marks_open` comment implies a `list_marks` order that contradicts the §7 prose.** `03 §4`'s index comment `idx_marks_open ON marks(resolved_at, created_at DESC)` reads as `ORDER BY resolved_at, created_at DESC`, which sorts the resolved group by `resolved_at` — but `§7` (and the `list_marks` row) require **"unresolved first then newest-first within each group"**. An internal spec inconsistency, not a code bug. | `specs/03_MASTER_PRODUCTION_SPEC.md §4` (idx_marks_open) vs `§7` | **✅ Resolved in PR6:** `list_marks` implements the **prose** order — `ORDER BY (m.resolved_at IS NOT NULL) ASC, m.created_at DESC, m.id DESC`. The schema DDL stays verbatim; `idx_marks_open` still index-serves the unresolved head (`WHERE resolved_at IS NULL ORDER BY created_at DESC`). Recorded so a future `§4` comment tweak keeps the prose authoritative (`07` #87). | ✅ resolved |
| 19 | P5 | **`UI_REFERENCE` named `ApiPanel` but not the Settings "Export…" affordance**, though `03 §7c` / the `§7` IPC table / `docs/0.3.0.md` PR7 all specify `export_data` + the button. An **incompleteness, not a contradiction** (no conflicting claim) — surfaced while implementing PR7's UI. | `specs/UI_REFERENCE.md §5` (Domain 0.3.0) vs `03 §7c`/`§7` | **✅ Fixed in PR7:** added an `ExportPanel` line to `UI_REFERENCE §5` (Settings "Data export" → `export_data`, JSON to Downloads, no images, works with the API off — D12), so the UI contract of record now names both the API panel and Export. | ✅ resolved |
| 20 | P5 | **`03 §2`'s "where the code lives" crate tree is stale — missing `api`, `mcp`, `doctor`, `textfilter`, `sysmon`, `uia`.** `§2` still lists only the 7 original crates; the workspace now has 13 (PR7 added `api`, PR8 adds `mcp`). The new crates are described in `§7c` prose, so this is a **stale diagram, not a contradiction** — surfaced while adding `crates/mcp`. | `specs/03_MASTER_PRODUCTION_SPEC.md §2` vs `Cargo.toml [workspace] members` | **✅ Resolved in PR9** (`feat/pr9-audit-release`): `§2` tree rewritten to the actual 13-crate workspace + `src-tauri` (adds `uia`, `textfilter`, `sysmon`, `doctor`, `api`, `mcp`), each with its shipped role; dependency-rule prose unchanged. | ✅ resolved |

> Resolved v0.1.0-era rows (#1–#4) → `specs/archive/06_PATCH_PLAN.v0.1.0.md` (ids preserved).
> Shipped 0.2.x rows (#5–#14 + the 0.2.1 build notes) → `specs/archive/06_PATCH_PLAN.v0.2.x.md` (ids preserved). Only the open upstream-leak row #15 stays live.

When the spec contradicts itself, stop, ask the user, and log the resolution here before coding.
