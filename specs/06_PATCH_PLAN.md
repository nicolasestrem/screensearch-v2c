# 06 — Patch Plan

> **Ordered fixes required before shipping**, plus any **spec contradictions** found during the
> build (`04 §5`). Empty until the build surfaces something.

| # | Priority | Issue | Source (spec §, file:line) | Fix | Status |
|---|---|---|---|---|---|
| 14 | P4 | **Event-driven capture review found stale docs and incomplete trigger-token test coverage.** After click/scroll-stop landed, `docs/ARCHITECTURE.md` still said they were deferred and still referenced schema v5, `docs/TESTING.md` said there was nothing to verify for mouse triggers, and `capture_trigger_db_str_round_trips` omitted the `Click` / `ScrollStop` variants even though production mapping handled them. | Review of `docs/0.2.0.md` event-driven capture; `docs/ARCHITECTURE.md`; `docs/TESTING.md`; `crates/traits/src/domain.rs` | **Applied (review follow-up):** Architecture now documents the full event-driven trigger set, schema v6, and the default-off `WH_MOUSE_LL` hook path for click/scroll-stop; Testing now includes manual acceptance for click and scroll-stop; `CaptureTrigger` round-trip coverage now includes `Click` and `ScrollStop`, and `from_db_str` has an exhaustive variant guard so future enum additions force this parser path to be revisited at compile time. No runtime/schema/IPC change. | ✅ applied |
| 5 | P6 | **Report context must scale with the time range, not run a flat single window (user decision 2026-06-25).** `03 §8b` modelled `generate_report` as a map-reduce over a relevance/recency selection; the user rejected a flat 8192-token window for a weekly report as "ridiculous" and required context to **scale with the range and guarantee temporal coverage** (every active day represented, a dense Monday must not starve a quiet Saturday). | `03 §8b`; `docs/0.2.0.md` PR6; `crates/kernel/src/reports.rs` | **Calendar-Grid Coverage Map-Reduce (CGCMR), applied.** Deviations from the literal `§8b` text, all covered by the user directive: **(a)** batching is a fixed **per-calendar-day grid** (one **or more** MAP passes per active period), not §8b token-budget batches — to *structurally* guarantee per-day coverage. A period holding more content than one 8192 window is split into pass-sized sub-batches before the MAP step (combining the grid with §8b-style token batching *within* a period), so a dense day — or a single-period Daily range, which is one group — fans out into several passes and the REDUCE folds them back rather than being truncated to one window. Within-period coverage is therefore complete, not merely per-period (added in the PR #33 review; the collapse-to-single-pass fast path now fires only when the whole report genuinely fits one window). **(b)** Daily/Weekly/Custom-without-prompt use **even ASC temporal striding** (`Store::sample_frames_in_range`) + chronological order, not best-first relevance; **Custom *with* a prompt** drives `hybrid_search(prompt, time_range)` (relevance) per the confirmed decision. **(c)** REDUCE may recurse **>1 level** for long ranges (consecutive `REDUCE_FANOUT`=6 groups, time order preserved), bounded by `MAX_REPORT_PASSES`=64. **(d)** the four settings keys are reinterpreted: `reports.daily_top_k` = **per-active-period frame budget**, `reports.weekly_top_k` = **global frame cap**; structural bounds (`MIN_FRAMES_PER_PERIOD`, `REDUCE_FANOUT`, `MAX_PERIODS`, `MAX_REPORT_PASSES`, `MAX_REPORT_CITATIONS`) are named constants (gap #60). **(e)** `report_progress` events + `cancel_report` are **additive** to the `generate_report -> ReportResponse` signature (the command still returns the value). **(f)** `n_ctx` stays **flat 8192** (the ctx-boost alternative was rejected: zero coverage benefit, forces a sidecar relaunch, no iGPU VRAM probe) — the *number* of 8192-bounded passes grows with the range, not the window. Honest-empty ranges issue **zero** sidecar calls. | ✅ applied |
| 6 | P3 | **PR7 report progress helper overlabelled Daily as Weekly.** During the PR7 UI audit, the Daily report progress state displayed "Weekly reports summarize each active day in turn..." even though the Daily tab was selected. | `ui/src/routes/Recall.tsx`; PR7 audit screenshot `19-daily-report-progress.png` | Changed the helper text to range-neutral bounded-pass copy: "Reports summarize active periods in bounded passes, so larger ranges can take a little longer." No IPC/schema/type change. | ✅ applied |
| 7 | P8 | **PR8 connection-count config + a new dep — minor scope notes (not contradictions).** `docs/0.2.0.md` PR8 frames the work as confined to `crates/inference/src/download.rs` but the project bans hardcoded magic numbers, and runtime sha256 integrity needs a hash impl. | `docs/0.2.0.md` PR8; `crates/inference/src/download.rs`; `Cargo.toml` | **(a)** Parallel-connection count is a documented `DOWNLOAD_CONNECTIONS` const (8) + `SSV2C_DOWNLOAD_CONNECTIONS` env override (clamp 1–16), **not** a `Settings` field — user-approved 2026-06-26; keeps the PR confined to `download.rs` and matches the file's existing const/env convention (no IPC/bindings/UI churn). **(b)** Added `sha2` as a **direct** dep (already in the tree transitively via hf-hub/rustls, so zero new compilation) for opportunistic LFS-sha256 verification. **(c)** Verification failure discards the partial (vs the network-error path that leaves it for resume), since every chunk is already marked done and a resume would re-fail. | ✅ applied |
| 8 | P3 | **PR3 strict attention-first acceptance still fails in live audit.** A 2026-06-26 audit of `6a484de..c1a3877` plus later PR6/PR7/PR8 text surfaces, using the real dev exe and existing app DB, found that default `content_text` search still returns static/app chrome for `Firefox`, `Steam`, `Deck`, `Recall`, and `GPU Memory`; a fresh Notepad capture also indexed `Firefox`, `Deck`, `Recall`, and `COMMAND` in default content. | `docs/0.2.0.md` PR3 acceptance; `03 §3b/§4/§8`; `docs/AUDIT_0.2.0_PR3_2026-06-26.md`; `07` gap #64 | **Resolved by #10 / `07` #66.** The later PR3 audit fix self-excludes ScreenSearch's own window, purges pre-existing own-window frames, backfills `FILTER_VERSION` 1→2, lowers the new-install chrome threshold, and hot-applies excluded-app capture changes. Residual rect-None / secondary-monitor chrome remains tracked in `07` #58. | ✅ resolved by #10 |
| 9 | P3 | **PR6 audit found doc drift, not behavior drift.** While auditing reports/Ask wiring, three stale-documentation surfaces showed up: `docs/ARCHITECTURE.md` still described the backend search limit as `1..=100` although PR6 report/custom retrieval can request larger bounded pools; the internal `ReportOutput.frames_summarized` doc comment still said it equals `cited_frame_ids.len()` even though PR6 tracks sampled/summarized frame counts separately; and audit markdown files are now intentionally ignored by `.gitignore`, so tracked roadmap/changelog links can point at local-only artifacts. | `docs/ARCHITECTURE.md`; `crates/traits/src/domain.rs`; `.gitignore`; `docs/0.2.0.md`; PR6 audit evidence | **Resolved / narrowed (PR7 follow-up).** `ReportOutput.frames_summarized` had already been corrected; this follow-up updates Architecture's search-limit wording and Testing's local-only audit policy. Ignored audit markdown remains local evidence, not tracked release documentation. | ✅ applied |
| 10 | P3 | **Fix for #8 — PR3 strict attention-first acceptance (2026-06-27).** Live replay of the shipped code over the audit corpus copy isolated the dominant default-search chrome as ScreenSearch indexing **its own window**, plus a cold-start window frozen by the no-backfill stance, plus the user-reported "Excluded Apps never applies". The classifier's explicit "rect unknown → suppress nothing" safety invariant means catalog/backfill cannot place rect-None desktop chrome. | `06` #8; `07` #64/#66; `crates/capture/src/{privacy,lib}.rs`; `crates/textfilter/src/lib.rs`; `crates/store/src/{schema,records,frames}.rs`; `src-tauri/src/lib.rs`; `crates/traits/src/{ipc,domain,contracts}.rs` | **Applied (user chose "self-exclude own window"):** (a) capture skips own-process foreground frames (`is_own_foreground_window`, PID-based) + a gated one-time purge of pre-existing own-window frames (`frames_with_app_hint` + `delete_frame`); (b) `FILTER_VERSION` 1→2 backfill (`backfill_filter_version`, replacing the catalog-wipe `reconcile_filter_version`) re-cleans sub-version frames via pure monotonic `textfilter::reconcile`, re-embedding changed frames — **reverses the no-backfill stance** (`07` #51/#52); (c) `chrome_suppress_min_seen` default 12→4; (d) `set_settings` reloads a running capture loop on `CaptureConfig` change (`Kernel::reload_capture`). Removed the now-unused `Store::reconcile_filter_version` trait method. Evidence (default content FTS, before→after): `Deck` 68→26, `Recall` 42→15, `Firefox`/`Steam` 24→15, `GPU Memory` 19→15; raw recovery + real work terms intact. **Residual:** rect-None/multi-monitor *other*-app desktop chrome (→ `07` #58). **PR #41 review hardening (Codex + Gemini, → `07` #67):** backfill now deletes the stale `source='ocr'` embedding in-tx so dropped chrome can't surface via the vector arm (Codex P1); the chrome catalog is cached by `app_hint` across the backfill (Gemini N+1); `purge_self_captures` `continue`s on a transient file-delete failure instead of orphaning the JPEG (Gemini). Two findings accepted as-is (user): 12→4 default reaches new installs only (no settings migration — Codex P2), `reload_capture` left non-atomic (unreachable UI race — Gemini). | ✅ applied |
| 11 | P3 | **PR7 no-evidence Ask label overstates context frames as citations.** The PR7 audit's unique-token refusal was honest, but the UI rendered unrelated retrieved context under `CITED FRAMES`, making context provenance read like support for a positive claim. | `docs/AUDIT_0.2.0_PR7_2026-06-25.md`; `07` #41/#63; `ui/src/components/domain/AnswerStream.tsx`; `ui/src/lib/ipc/useAsk.ts`; `crates/inference/src/answer.rs` | **Applied (user chose relabel approach).** Keep the existing typed IPC stream and model prompt; relabel the Ask frame tiles to `Frames checked` and update comments to clarify that these ids are reviewed context/provenance, not model-authored claim-level citations. No schema, migration, binding, or prompt heuristic change. | ✅ applied |
| 13 | P5 | **Spec deviation: 0.2.1 enrichment throttle monitors the GPU via Windows PDH GPU-Engine counters, not NVML.** `docs/0.2.0.md` (former PR5) frames GPU utilization as "NVIDIA/NVML-only, so a weak iGPU laptop is CPU-pressure-only." The user chose the broader, vendor-neutral path instead. | `docs/0.2.0.md` "Deferred work (0.2.1)" smart-enrichment-throttle item; `07` #49; `crates/sysmon/src/gpu.rs` | **Applied (user explicitly chose "Universal native (PDH)", 2026-06-29).** GPU utilization reads Windows PDH `\GPU Engine(*)\Utilization Percentage` via `PdhAddEnglishCounterW` (English counter name → locale-proof), summed across engines and clamped to 100 — so **any** GPU vendor (NVIDIA/AMD/Intel iGPU) is monitored, strictly better coverage than NVML-only. Truthfulness is preserved: if the GPU perf-counter provider is absent (VM / disabled / unsupported driver) the probe latches `gpu_monitored=false` once, reports `gpu_pct=None`, and the throttle runs on **CPU pressure alone** — surfaced honestly in the UI as "GPU not monitored". CPU uses `GetSystemTimes` (whole-machine busy%). Tracked as an authorized deviation, not a defect. | ✅ applied (deviation, authorized) |
| 12 | P4 | **Spec contradiction: 0.2.1 click/scroll-stop use `WH_MOUSE_LL`, which the roadmap steers away from.** `docs/0.2.0.md` lists **click** + **scroll-stop** as deferred-remainder triggers but its hook-strategy guidance "deliberately steers away from" a low-level mouse hook (input-latency + invasiveness), and the part-1 note (this file, 2026-06-28) deferred them ≥0.2.2 for exactly that reason. PR4 part 2 lands them, contradicting that deferral. | `docs/0.2.0.md` "Deferred remainder (≥0.2.2)"; `06` 2026-06-28 part-1 note; `07` #47; `crates/capture/src/events.rs`; `crates/capture/src/trigger.rs` | **Applied (user explicitly authorized the deviation, 2026-06-28).** Both triggers are **default OFF** behind `capture.event_on_click` / `capture.event_on_scroll_stop`; the global `SetWindowsHookExW(WH_MOUSE_LL, …)` is installed on the existing message-pump thread **only when** one is enabled. The latency risk the roadmap cited is mitigated structurally: the hook callback reads **only the message id** (which button / wheel) — it **never** touches `MSLLHOOKSTRUCT.pt`/coordinates — and does only a non-blocking `try_send` before `CallNextHookEx`, so it can't inject perceptible system-wide input latency (privacy + latency both addressed). The remaining "is there perceptible lag?" check is a local live item (`cargo test -p capture -- --ignored` lifecycle stress + a dev-session feel check). Tracked as a deviation, not a defect. | ✅ applied (deviation, authorized) |

| 15 | P5 | **Upstream llama.cpp multimodal host-memory leak (build 9842).** ~149 MB committed host RAM leaked per vision inference; VRAM flat. Mitigated locally by the `sidecar.recycle_*` valve (recycle at a committed-RAM ceiling). Real fix requires a newer bundled `llama-server` build. | `07` #72; `specs/03_MASTER_PRODUCTION_SPEC.md §8`; `crates/inference` sidecar lifecycle | **Open (upstream).** Local mitigation shipped (`sidecar.recycle_enabled` default true, `sidecar.recycle_rss_mb` default 0=auto). Upgrade the bundled llama-server build when a fix lands upstream; re-run the 60-frame isolated test to confirm RSS stays flat. | open (upstream) |

> Resolved v0.1.0-era rows (#1–#4, 2026-06-21..2026-06-24) archived to `specs/archive/06_PATCH_PLAN.v0.1.0.md` (original ids preserved).

**2026-06-28 — 0.2.1 event-driven capture (`feat/0.2.1-pr4p1-event-capture`): no spec contradictions
found.** The work implements the `docs/0.2.0.md` "Deferred work (0.2.1)" event-driven-capture item
and resolves `07` #47 as specified — opt-in, default off, `SetWinEventHook` + `AddClipboardFormatListener`
over low-level keyboard hooks, no stored keystrokes/clipboard contents. Two pre-existing decisions
the spec left open were honored, not contradicted: (a) **click + scroll-stop deferred ≥0.2.2** because
they need `WH_MOUSE_LL`, which the roadmap steers away from (the kickoff listed them but the
hook-strategy guidance disfavors them — recorded in `07` #47, not a contradiction); (b) the
`CaptureTrigger` field now exists and is persisted, but the attention filter stays trigger-agnostic
by design (`07` #57). No new patch rows required.

**2026-06-28 — 0.2.1 PR4 part 2 (`feat/0.2.1-pr4p2-uia-and-mouse-triggers`): two authorized roadmap
deviations, both recorded.** (1) **`WH_MOUSE_LL` for click/scroll-stop** — the roadmap steers away
from it; the user explicitly authorized it. Logged as patch **#12** above (default-off, hook installed
only when enabled, callback reads only the message id and `try_send`s, never coordinates) — this
supersedes the part-1 note's "deferred ≥0.2.2" stance for those two triggers. (2) **UIA text ships
default ON** rather than opt-in — the user's explicit choice; recorded in `07` #48 (it raises the bar
on the capability probe, the per-frame latency budget, and the OCR fallback, since the greenfield path
then runs on every frame from first launch). Per the rule below, both were surfaced to the user and
authorized before coding; they are deviations, not self-contradictions.

When the spec contradicts itself, stop, ask the user, and log the resolution here before coding.
