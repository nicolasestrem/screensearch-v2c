# Rust
/target/
**/target/
*.pdb

# Node / frontend
node_modules/
dist/
.vite/
*.local

# Tauri
src-tauri/target/
src-tauri/gen/
# Staged externalBin sidecar (built by scripts/stage-mcp.mjs; never commit — 0.3.0 PR8).
src-tauri/binaries/

# Models & data (downloaded at runtime — never commit)
.models/
*.gguf
*.onnx
*.safetensors

# Local databases, logs, captures
*.db
*.db-wal
*.db-shm
*.log
captures/
docs/AUDIT*.md
docs/audits/
# 0.4.0 PR2 ground-truth exports + hand labels (personal screen history — never commit)
/harness-data/

# Environment / secrets
.env
.env.*
# Updater signing keys (minisign; 0.3.2 PR2). The private key lives outside the repo
# (a CI secret + an offline backup, D2); this is a belt-and-braces guard so key material
# can never be committed if generated inside the tree. The public half is baked into
# tauri.conf.json instead.
*.key
*.key.pub

# Verification artifacts (Playwright-MCP snapshots / screenshots — not source)
.playwright-mcp/
m1m2-*.png
m3m4-*.png

# Agent scratch (subagent-driven-development progress ledger — local only)
.superpowers/

# OS / editor
Thumbs.db
desktop.ini
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
/.codex
custom-llm-training.md
docs/0.3.0-kickoff-prompt.md
docs/0.3.1-kickoff-prompt.md
docs/0.3.2-kickoff-prompt.md
docs/0.4.0-kickoff-prompt.md
WATCHDOG.yml
