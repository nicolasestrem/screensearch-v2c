{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: core APIs only (no plugins yet in P0).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
